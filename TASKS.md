# Know Your Vote Kentucky (KYvKY) — Task Tracker

> This file is the source of truth for roadmap tasks.
> Update it as work is completed or priorities shift.
> It lives in the repo so any AI with file access can read and act on it.

---

## In Progress

_Active development only. Shipped work moves to Recently completed; deferred/revisit items are grouped under "Deferred follow-ups" below and in Backlog._

### Open near-term items

- [ ] **Filters — "Clear filters" button + de-emphasize the Filters button.** The one unshipped item from the 2026-07-24 dictated UI-fixes batch. A "Clear all" chip already exists on `/bills`, `/members`, `/committees`, `/meetings`, and `/search`, so step one is a product call: restyle the existing control or add a new one, and which "Filters" button is meant.
- [ ] **Signed-in mobile a11y sweep** — profile/feed/follow flows; needs an authenticated session (carried from 2026-06-01 and 2026-06-12).

### Accuracy + health-check hardening — follow-ups (2026-07-31, PRs #214-#218)

**Next-agent brief with verification queries, sizing, and the gotchas: [docs/handoff-data-defects-2026-07-31.md](./docs/handoff-data-defects-2026-07-31.md).** Rationale and evidence for each is in [decisions.md § 2026-07-31](./decisions.md) (three entries). Ordered by user impact.

> **Status 2026-07-31 (evening).** Defects 1 and 3 are shipped and verified in production; defect 2's backfill is running. **Defect 4 is the next resolution** — see the dedicated item directly below. Two new follow-ups surfaced during the work and are filed at the bottom of this section.

#### ▶ Next up

- [ ] **Defect 4 — `/bills` browse: replace the unbounded exact `count`. NEXT RESOLUTION.** The last of the four defects and the only one still untouched. Cause is confirmed, not hypothesised: `EXPLAIN ANALYZE` shows the ORDER BY is already served by `idx_bills_session` via an incremental sort (3.7 ms warm), while the exact `count()` is a **`Seq Scan` over the whole 84 MB heap** — ~11 ms warm but **4,254 ms** cold, past the 3 s anon `statement_timeout`. That variance is why failures are intermittent (~11/day, 38 distinct users) and why they grow with every sync cycle. **Do not ship the composite `(session, last_action_date)` index** — that hypothesis is refuted and it would add write cost on every sync for no gain. Full profiling evidence in the ops-notes bullet below. **Blocked on one product call**, deliberately not made unilaterally because it changes a user-facing number:
  - *(a)* leave it exact — accepts the timeouts;
  - *(b)* `count: 'planned'` on the **unfiltered** path only (estimate 23,426 vs actual 22,547, 3.9% off), keeping exact counts on filtered paths, which are selective and cheap;
  - *(c)* **exact but bounded** — count to a cap (e.g. 1,000) and render "1,000+". Removes the unbounded cost without ever displaying a wrong number; the recommended option for a civic-data site where a visibly drifting count undercuts trust.

  Also worth doing regardless: `pg_stat_user_tables.last_analyze` was null for `ky_bills`; a manual `ANALYZE` has since been run, but it is not scheduled.

#### Shipped / in flight

- [x] **Committee materials — delete 802 duplicate rows with a dead URL. ✅ Done 2026-07-31.** Probed live from Actions first (802/802 legacy URLs dead, 801/802 twins alive) rather than trusting the code comment that claimed the 404s; migration 048 applied, 1,773 → 970 rows, all nested rows intact, deleted rows preserved in `ky_committee_materials_legacy_dupes_048`. A 40-row sample had returned a clean verdict and **missed** the one dead twin the full 802-row pass caught — probe all of it before deleting any of it.
- [x] **`ky_votes.nv_count` — root cause fixed 2026-07-31.** Cause was a dropped field in the dataset import mapper, not sync coverage — see the retraction in "New follow-ups" below for the full story. Remaining step: run the dataset sync with `--force` to repopulate the existing 6,944 rows (~13 LegiScan calls). The per-bill `backfill:vote-nv-counts` script remains in the repo as a targeted fallback but should **not** be the first resort — it costs ~6,944 calls to do the same job.
- [x] **Re-sync 12 meetings with unresolved agenda bill refs. ✅ Done 2026-07-31.** Root cause was not stale rows: `lrc-bill-reference-parser.ts` failed on a parenthetical session suffix. Fixed at the parser, verified against all 209 stored agenda strings, 12 rows repaired; `bill_session_label IS NULL` is now 0. The 5 remaining unlinked refs are `BR nn` bill requests and correctly have no `ky_bills` row.

#### Still open

- [ ] **Verify committee meetings beyond the live calendar window.** The LRC weekly calendar is the only upstream comparison for meetings and covers just the current week; the new corpus invariants prove internal consistency, not agreement with LRC. 146 upcoming and all Wayback/PDF-backfilled meetings have no upstream check. Needs a different source — committee profile pages or the interim calendar PDF.
- [ ] **Stateful sampling rotation (`last_audited_at`).** Accuracy sampling is still memoryless (~0.18%/run); the active-window bias helps but gives no coverage guarantee. `ky_accuracy_findings` (migration 046) is the substrate for building it.
- [ ] **Confirm `lrc-enrollment-actions` goes green.** The 404-vs-failure classification fix shipped in #216; the next Vercel cron run (`45 14 * * *` UTC) is the first to exercise it. If it stays red, the 11 errors were real rather than expected 404s and need investigating.

#### New follow-ups surfaced 2026-07-31 (found while fixing defects 1–3)

- [x] **✅ Fixed 2026-07-31 — `nv_count` was never a sync-coverage problem; the dataset import silently dropped the field.** Filed here first as "the votes cron only covers 5 bills/day, so the corpus is frozen" — **that framing was wrong and is retracted.** Vote data is *not* frozen: `sync-ky-dataset.ts` re-imports whole sessions (bills + people + roll calls) from the LegiScan bulk dataset weekly, gated on `dataset_hash`, and an unchanged hash means upstream genuinely has no new data. That path — not the `limit=5` cron — is what wrote all 6,944 vote rows. **The real bug was one missing line:** `buildVoteRow` (`src/lib/ky-legiscan-dataset-import.ts`) mapped `yea`/`nay`/`absent`/`passed`/`votes` but not `nv`, so `nv_count` stayed NULL on every row while `absent_count` — its sibling in the same payload — was 100% populated. Fixed at the mapper, plus a `--force` flag on the dataset sync (and a `force` input on the weekly workflow) to re-import unchanged sessions, which is exactly the case a mapper fix creates: identical upstream payload, but we now read a field we previously discarded.

  **The cost lesson is worth keeping.** The per-bill API backfill this defect appeared to need was ~6,944 `getRollCall` calls (~23.1% of monthly quota, hours of wall-clock). Re-importing the same data from the dataset path is **~1 call per session, ~13 total.** Before spending quota per-bill, check whether the bulk dataset already carries the field.

  Residual, low priority: the `?source=votes&limit=5` cron now serves only intra-week freshness for the most-recently-actioned bills, with the dataset sync as the corpus-wide path. Its `limit=5` is not a correctness ceiling. Worth revisiting only if intra-week vote latency becomes a complaint.
- [ ] **🟡 MEDIUM — 4 committee materials are confirmed dead with no replacement.** Distinct from the 802 duplicates migration 048 removed: these have **no nested twin**, so they are not duplicates and there is nothing to fall back to — the document is simply gone from LRC. They carry `link_status = 'dead'` from the accuracy audit and currently render as dead links. Needs a product call rather than a cleanup: delete the rows, keep them but render as unavailable rather than as a link, or try Wayback for an archived copy (`backfill-lrc-calendar-wayback.ts` is the existing precedent). Low volume, so low urgency — but they are user-visible dead links on a civic-data site, which is exactly the class of thing `docs/voice-and-tone.md` says to be honest about.

  ```sql
  select id, title, meeting_date, url from ky_committee_materials where link_status = 'dead';
  ```

### Deferred follow-ups (flagged, revisit when there's appetite)

- [ ] **Digest email progress meter — placement/density review.** The 4-stage meter renders on **every** bill block in the digest (`bill-digest-email.tsx` / `run-bill-digest-cron.tsx`). Decide whether to gate it (e.g. only on major-milestone events). No functional issue — a product/voice call; shipped as-is (risk accepted).
- [ ] **Session-date verification — medium-confidence + gaps (risk accepted).** Two backfilled sine-die dates rest on secondary sources only — **2021 RS end (2021-03-30)** and **2016 RS end (2016-04-15)**. Three sessions were omitted because their exact dates couldn't be confirmed from this environment (LRC pages 403 on egress): **2011 RS** sine die, **2011 Special**, **2012 Special** (labels still exist in `KY_BILL_SESSION_OPTIONS` for filtering). Authoritative source: LRC "Extraordinary Sessions since 1940" + "Normal Effective Dates" pages; verify from an unrestricted network and add the three to `KY_SESSIONS` in `src/lib/ky-sessions.ts`.
- [ ] **Design-system primitive extraction (larger refactor, agreed 2026-07-21).** Quick wins are done; the deferred piece is extracting shared `ActiveFilterChip` / `FilterSelect` / `SearchInput` (+ `SegmentedControl`) into `src/components/ui/` and migrating every call site (the active-filter chip blocks are reimplemented inline across 5 browse surfaces; the 6 search boxes diverge). Code consolidation, not a visible fix — sequence it when there's appetite for the broad diff.
- [ ] **Design system v1.2 — dense table / list archetype (agreed 2026-07-22).** The power-user ICP (advocacy staffer scanning 1,400+ bills) has no sortable/paginated dense-table spec — current surfaces use the card grid + `PaginatedSection`. Out of scope for v1.1; specify in v1.2. Tracked in `design-system/audit.md` (open decision #4).
- [ ] **AI summaries — cross-bill audience lens pages** (FEEDBACK #3 proper).
- [ ] **Operator — PostHog prod key.** Create a PostHog personal API key (Query Read scope) and set `POSTHOG_PERSONAL_API_KEY` / `POSTHOG_PROJECT_ID` in Vercel production to activate the "Most viewed bills" unique-visitors ranking (falls back to cumulative `ky_bills.view_count` without it).

### Ops notes — resolved (2026-06-23 → 2026-07-06)

Daily health-check findings from this window, all closed (full rationale in decisions.md where linked): accuracy-audit timeout + quota-stop mis-page ([§ 2026-06-27](./decisions.md#2026-06-27--mid-run-legiscan-quota-hold-classified-as-skipped-accuracy-audit-timeout-raised) / [§ 2026-06-28](./decisions.md#2026-06-28--accuracy-audit-legiscan-quota-stop-is-a-skip-not-an-operational-error)); `fetchMasterListRaw` cache fallback; `@react-email/*` → unified `react-email` 6.x; npm audit → 0 vulnerabilities; legislator-links-weekly red (06-29) closed by PR #118; Vercel MCP team-grant gap re-authorized 07-06 ([§ 2026-07-06](./decisions.md#2026-07-06--vercel-mcp-connector-gap-root-cause-found-connector-lacks-the-eighth-dimension-team-grant)); LegiScan API timeout blips (07-01→03) self-healed, watch closed.

- [ ] **⚠️ Still open (manual, Sentry UI):** configure `route:cron/notify` + `route:webhooks/resend` alert rules — also tracked on [docs/launch-checklist.md](./docs/launch-checklist.md). Still the only Vercel cron with no active alerting: the 2026-07-31 pass added Sentry cron monitors for the sync routes and a second health-check path, but `/api/cron/notify` has no Slack path and its tags are emitted with nothing listening.

### Ops notes — `/bills`-browse runtime-error findings (2026-07-07 → 2026-07-20) — resolved

Both `/bills`-browse runtime-error patterns shared one root cause: **Finding #1** (empty-message `Error: {"message":""}` on `/bills`, `/bills/house`, `/bills/senate`) and **Finding #2** (`unstable_cache` revalidation logging bucketed as an error, spread to `/bills/[id]` / `/committees/[slug]` / `/members/[slug]` detail keys) were both the browse fetch throwing Supabase's raw `PostgrestError` — a blank PostgREST message (statement timeout `57014`) serialized as `{"message":""}` with no stack. **✅ Fixed 2026-07-20:** `fetchKyBillsBrowsePageUncached` now wraps DB errors in a real `Error` (stage + PostgREST code/details/hint + original as `cause`) and captures to Sentry at the origin with full query context (`src/lib/ky-bills-browse-server.ts`); verified in production, no recurrence. The intended-diagnostic replacement (`ky_bills browse query failed [count]`) is now what surfaces on the rare blank-PostgREST event — watched below. Daily detail + orphan-branch root cause: [decisions.md § 2026-07-18](./decisions.md#2026-07-18--daily-system-health-check-routine-orphan-branch-pileup).

### Ops notes — daily health checks (2026-07-19 → 2026-07-31)

Daily health-check log for this window collapsed to open/watched items only (resolved detail lives in decisions.md and the ops-notes summaries above).

- [x] **`legislator-links-weekly.yml` run #18 (2026-07-20) — resolved; was a false alarm (transient LRC blip, not broken links).** Triaged 2026-07-26. Run #18's 79 "failures" were **all `lrc_profile_url` probes returning HTTP 503 / status-0** from `legislature.ky.gov` under the verifier's concurrent load — transient signals, not 4xx dead links (verified from the `#errors` post). Run #18 posted at 2026-07-20 14:03 UTC, **47 min before** the transient-exemption fix (`202f383`, 2026-07-20 14:50 UTC — decisions.md § 2026-07-20) that classifies 503/status-0 as *skips*; it ran on the pre-fix code, which is why the blip counted as `failed`. Confirmed by a fresh `workflow_dispatch` on the fixed `main`: **run #19 (2026-07-26) — 141 legislators · 421 probes · Failed 0.** No genuine broken links; no content triage needed.
- *Optional hardening (not blocking):* when `legislature.ky.gov` throttles under the CONCURRENCY=6 probe burst, LRC links are correctly skipped rather than failed, but that run can't verify them — consider a lower per-host concurrency / gentler pacing for `legislature.ky.gov` so coverage doesn't drop during a throttle. Low priority.
- **Watching → escalating — `ky_bills browse query failed [count]` intended-diagnostic error.** **43 events over 07-24→07-30** (38 distinct users), up from 21 over 07-24→07-28 — daily rate went ~5.25/day → ~11/day, spreading from member/meeting detail routes to `/bills` and `/api/bills/browse` directly. Same diagnosed shape as the `get_votes_for_legislator` timeout PR #212 fixed on a different table (cost scaling with data volume, not the request) — flagged 07-30 as a hypothesis worth profiling. **2026-07-31 — no fresh telemetry (Vercel MCP tools didn't load this session — see the MCP-gap bullet below), but code review turned the hypothesis into a concrete candidate:** the default no-filter browse path (`fetchKyBillsBrowsePageUncached` in `src/lib/ky-bills-browse-server.ts`) does `ORDER BY session DESC, last_action_date DESC NULLS LAST` plus an exact `count()`, both over the *whole* `ky_bills` table when no chamber/topic/session filter is applied — and `supabase/migrations/002_indexes_and_rls.sql` only has single-column indexes on `session` and `last_action_date` (`idx_bills_session`, `idx_bills_last_action_date`), no composite covering both sort keys. Without a composite index Postgres can't satisfy that two-column ORDER BY from an index and has to sort the matching rows directly, which gets more expensive every sync cycle as the current session's row count grows — the same "scales with table growth, hits the 3s anon `statement_timeout`" shape as the `ky_votes_roll_call_gin` fix (`supabase/migrations/045_get_votes_for_legislator_perf.sql`). **Not shipping a migration on this hypothesis alone** — no `EXPLAIN ANALYZE` access from this session (no Supabase MCP/DB connector available) to confirm the planner is actually falling back to a full sort rather than something else (e.g. lock contention, connection pool exhaustion). **2026-07-31 (later) — profiled against production; the composite-index hypothesis is REFUTED and the cause is the `count`, not the sort.** `EXPLAIN ANALYZE` on the default no-filter path shows the ORDER BY is **already satisfied from the existing single-column index**: `Index Scan Backward using idx_bills_session` feeding an **Incremental Sort** with `Presorted Key: session`, top-N heapsorting only within the leading session group (1,738 rows) — 3.7 ms warm. A composite `(session, last_action_date)` index would remove a sort of ~1,738 rows and change essentially nothing; **do not ship it** (it would add write cost on every sync for no gain). The exact `count()` is the real cost: **`Seq Scan on ky_bills`** over the whole 84 MB heap (22,547 rows / 5,785 buffer pages). Warm it is ~11 ms, but a cold/contended run measured **4,254 ms** — past the 3 s anon `statement_timeout`. That variance is why failures are intermittent (~11/day) rather than constant, and the cost grows with every sync cycle. **Recommended fix (not yet shipped — changes a user-facing path, wants a product call):** use PostgREST `count: 'planned'` for the **unfiltered** browse path only (planner estimate 23,426 vs actual 22,547 — 3.9% off, fine for pagination), or cache the unfiltered total; keep exact counts on filtered paths, which are selective and cheap. **Also noticed:** `pg_stat_user_tables.last_analyze` is **null** for `ky_bills` (last autovacuum 2026-07-22, 1,525 dead tuples) — a manual `ANALYZE` is worth doing regardless.
- **New 2026-07-31 — Vercel MCP tools absent from this session (recurring observability gap).** `ListConnectors` reports Vercel as installed and `enabledInChat: true`, but no `mcp__Vercel__*` tools actually appeared this session (checked via targeted tool search on deployments/projects/logs keywords) — so **no Vercel deployment status or runtime-log/error counts could be pulled this check**, same class of gap as 2026-06-09, 2026-06-23, and 2026-07-20 (all self-healed previously; 2026-07-06 traced one occurrence to a missing team grant, decisions.md § 2026-07-06). GitHub Actions remained checkable directly and all 6 workflows are green on their latest runs (`accuracy-audit` 07-26 success, `sync-ky-bills-status` 07-31 08:53 UTC success, `sync-lrc-calendar` 07-30 success, `legislator-links-weekly` 07-27 success, `legiscan-dataset-weekly` 07-26 success, `slack-repo-events` 07-30 success); 0 open GitHub issues; 1 open PR (`#213`/this rolling health-check PR). **If this keeps recurring, it's worth moving Vercel/Sentry runtime-error checks to a manual dashboard cadence** (as floated 2026-06-23) rather than depending on the MCP connector being loaded each run.
- **Resolved this window:** `accuracy-audit.yml` run #11 (07-19 WARN-level content findings — mis-tagged topics HB84/HB142/SR35 + two glossary defs) cleared by run #12 (07-26, content triage happened between runs); findings #1 (`/bills*` empty-message) and #2 (`unstable_cache` revalidation logging) confirmed fixed in production (2026-07-20, no recurrence); the 07-19 Wayback `ECONNREFUSED` blip and the 07-20 "no Vercel tools at all" MCP observability gap both self-healed. CI otherwise clean; 20/20 recent Vercel deployments `READY` as of the last check with Vercel access (07-30), none failed.

**Roadmap priority (2026-06-11):** [operator launch checklist](./docs/launch-checklist.md) (Resend DKIM, Sentry alerts, legal review, email QA) → mobile a11y profile/feed/follow sweep (deferred from 2026-06-01 scope). Phase 5b enrollment-actions **shipped 2026-06-11** — see Recently completed.

## Maintained on autopilot

- **Daily ops Routines (Claude Code scheduled triggers, not GH Actions)** — two Routines fire sessions into this repo daily: **"System health check"** (12:00 UTC, env `env_011CUP9uJd4u7xGxxR4Sw2EE`, Vercel MCP + autofix-on-PR) and **"kyvky.com accuracy spot check"** (13:05 UTC, env `env_014KLPBSZr3RyQKWV8tu8QhR`). **Output-handling protocol (health check), set 2026-07-18 to stop the orphan-branch pileup:** record a finding only when it is new/actionable; commit to the single rolling branch `chore/daily-health-check` and open/update **one** PR against `main` (never a fresh per-run branch); on an all-nominal day, write nothing to git and just report in-session. Full rationale + the canonical Routine prompt: [decisions.md § 2026-07-18](./decisions.md#2026-07-18--daily-system-health-check-routine-orphan-branch-pileup). The Routine prompt itself must be edited in the Claude Code web UI (the scheduling API can update name/cron/enabled/model but not the prompt; recreating the trigger would drop the Vercel MCP grant + autofix).
- **Legislator outbound links** — `.github/workflows/legislator-links-weekly.yml` runs Mondays 12:00 UTC: `sync:ky:legislators` → `verify:legislator-links --json` → `audit:legiscan-subjects --json`, uploading `reports/` as artifacts. Manual fallback: `npm run verify:legislator-links`. Required GH secrets: `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `OPENSTATES_API_KEY`, `LEGISCAN_API_KEY` (last optional). **Slack:** sync + verify digests when `SLACK_WEBHOOK_*` + `SLACK_SYNC_NOTIFY_CLI=true` (posts on change/error only).
- **Bill AI summaries (beta)** — `sync-ky-bills-status.yml` runs `backfill:bill-summaries --limit=300` after each clean 6h sync, regenerating `ai_summary` for new/changed bills (input-hash mismatch) or gaps. Advisory (`continue-on-error`), Anthropic-only (no LegiScan quota), skipped without `ANTHROPIC_API_KEY`. Active session verified at 100% coverage 2026-06-28. Manual top-up / new-DB bootstrap: `npm run backfill:bill-summaries` (no limit).
- **GitHub Actions sync Slack** — `sync-ky-bills-status.yml` (every 6h), `sync-lrc-calendar.yml` (2× daily live + weekly Wayback backfill), `legislator-links-weekly.yml` post to `#status-reports` on success and `#errors` on failure. Optional repo secrets: `SLACK_WEBHOOK_STATUS_REPORTS`, `SLACK_WEBHOOK_ERRORS`, `SLACK_WEBHOOK_SYNC`, `SLACK_WEBHOOK_URL`. Posts on change/error only (heartbeat mode retired 2026-06-07 — see decisions.md).
- **Email digest** — `/api/cron/notify` runs daily 11:00 UTC via Vercel Cron (weekly users batched on Mondays). Suppressed users skipped. Failures + per-user-error spikes captured in Sentry (tags `route:cron/notify` and `route:webhooks/resend`).
- **Committee meeting materials** — `/api/sync?source=lrc-committee-materials` runs daily **13:30 UTC** via Vercel Cron. Scrapes `apps.legislature.ky.gov/CommitteeDocuments/{lrc_rsn}` for every committee with `lrc_rsn`, upserts into `ky_committee_materials`. Idempotent via `(committee_id, url)`. Status tracked in `ky_sources`. **Historical backfill** (one-time per env): `npm run backfill:lrc:committee-materials` walks each committee's `Other Meeting Years` chain — primary applied 2026-06-11 (12 inserted, 1,002 updated across 69 committees).
- **LRC enrollment / executive actions** — `/api/sync?source=lrc-enrollment-actions` runs daily **14:45 UTC** via Vercel Cron. Parses `apps.legislature.ky.gov/record/{session}/enrollment_actions.html` → date-stamped `ky_bill_status_history` rows (`signed_or_vetoed`, `veto_override_attempt`) with `observed_at = action_date`. Zero LegiScan quota. Manual: `npm run sync:ky:lrc-enrollment-actions`. Initial backfill 2026-06-11: **466 rows** (2026 + 2025 RS). See [session-record-spike-report.md](./docs/specs/session-record-spike-report.md) + decisions.md § 2026-06-11.
- **Outbound mail policy** — `From: alerts@kyvky.com` (transactional only, do not reply); `Reply-To: katie@kyvky.com` (real inbox). All inbound contact / vulnerability reports go to `katie@kyvky.com`. Webhook deliveries from Resend → `https://www.kyvky.com/api/webhooks/resend` (apex 307s break POST). Plain-text fallback included on every transactional send.
- **Content accuracy audit** — `.github/workflows/accuracy-audit.yml` runs Sundays 07:00 UTC (+ `workflow_dispatch`): `audit:accuracy`. Hybrid agent — deterministic diffs (bills/sponsors vs LegiScan `getBill`, roll-call tallies vs `getRollCall`, legislators/contacts vs Open States, committee-material + agenda drift vs live LRC, static link-shape validation) plus an opt-in LLM pass (`--no-llm` to skip). **Seeded random sampling:** each run checks a different slice (seed printed as `seed=…`; pin with `--seed=N` or `ACCURACY_SEED` to reproduce). Slack via `notifyAccuracyAuditSlack` (status digest always; `#errors` on hard failures). Tuning knobs: `ACCURACY_BILLS_LIMIT`, `ACCURACY_VOTES_LIMIT`, `ACCURACY_LINK_SAMPLE`, `ACCURACY_MATERIALS_COMMITTEE_LIMIT`, `ACCURACY_LLM_SAMPLE`, `ACCURACY_PROBE_LINKS`, `ACCURACY_LEGISCAN_QUOTA_STOP_PCT`. Manual: `npm run audit:accuracy:dry`. See decisions.md § 2026-06-03.

## Operator launch checklist

Consolidated into **[docs/launch-checklist.md](./docs/launch-checklist.md)** (2026-06-02) — single source of truth for the not-blocking-but-recommended-before-launch items (Resend DKIM, Sentry alerts, inbox routing, legal review, email-client QA, Vercel env cleanup, regression cadence).

## Handoff — next agent (committee follow v1.5 + Wave 3)

Use this when continuing **Follow committees v1.5**, **Wave 3 committee/data**, or **launch operator checklist**.

### Context (already in repo)

- **Auth:** Supabase with `@supabase/ssr` — cookie-backed browser client (`src/app/lib/supabaseClient.ts`), middleware session refresh (`src/lib/supabase/middleware.ts`, `src/middleware.ts`), `UserContext`, auth routes under `src/app/auth/`, centered layout `src/app/auth/layout.tsx` + `AuthPaperLayout`.
- **Bill follows:** Migration **019** — `ky_bill_follows`, `ky_notification_preferences`, `ky_bill_status_history`, `ky_notifications_log` + RLS. API: `GET/POST/DELETE /api/bills/[id]/follow` (Bearer token via `getAuthedUser`). Client: `FollowBillButton` on `/bills/[id]`.
- **Committee follows (v1 — PR #38):** Migration **026** — `ky_committee_follows` + `ky_committee_events`. API: `GET/POST/DELETE /api/committees/[id]/follow`. Client: `FollowCommitteeButton` on committee detail; bookmark toggle on `KYCommitteeCard`; `ProfileFollowedCommitteesSection` on `/profile`. Digest: `committee_meeting_scheduled` opt-in on `/profile`; LRC sync emits `meeting_scheduled` on new meetings. List: `GET /api/me/follows` returns bills + committees.
- **Digest / email:** M6–M8 **complete** — `/api/cron/notify`, Resend, bounce webhook, welcome email, `preview:digest` harness. Two Resend uses: (1) Supabase Auth SMTP; (2) app digests via `RESEND_API_KEY` / `RESEND_FROM_EMAIL`.

### Ops / env

- **Migration order (new projects):** **016 → 017 → 018 → 019 → 020 → 021 → 022 → 023 → 024 → 025 → 026 → 027** (`npm run db:apply-sql` or SQL editor). **Primary environment:** **016–027** applied (026 pre-existing; **027** applied 2026-05-26).
- After **027** on a fresh DB: run `npx tsx scripts/backfill-interim-calendar-2026.ts` (idempotent; seeds interim meetings from LRC PDF). Primary already has meetings (214 rows as of 2026-05-26).
- Vercel: `RESEND_WEBHOOK_SECRET`; canonical `APP_PUBLIC_URL` / `NEXT_PUBLIC_APP_URL` = `https://www.kyvky.com` (apex 307 breaks webhook POST).
- `npm run preview:digest -- --email <addr>` — add `--inject HB1` for synthetic bill events; supports committee sections when user follows committees + opts into **Committee meeting scheduled**.

### Suggested next implementation order

1. **Follow committees v1.5** — `/meetings?follows=me`; committee events in `GET /api/me/activity`; `agenda_updated` / `meeting_cancelled` in LRC sync (v1 only emits `meeting_scheduled`). See **Backlog → Follow committees v1.5**.
2. **Manual email QA** — [docs/email-client-qa.md](./docs/email-client-qa.md).
3. **Wave 3** — committee materials sync, session record spike, interim/milestone banners ([committee-calendar.md](./docs/specs/committee-calendar.md) Phase 5+).
4. **Operator launch checklist** — Resend SPF/DKIM, Sentry alert rules, legal review of `/privacy` + `/terms`.

### Files to read first

- [docs/specs/follow-bills.md](./docs/specs/follow-bills.md) — bill follow UX and phases.
- [decisions.md](./decisions.md) § 2026-05-26 — committee follows v1 decisions.
- PR **#38** on GitHub — full committee-follow ship list.

## Operator checklist

- **Database migrations** — Full order: **016 → 027** (see Handoff). Highlights: **024** before first `npm run sync:ky:lrc-calendar`; **025** for saved searches + bill snooze; **026** for committee follows + event log; **027** seeds 2026 interim/statutory committees. **Primary environment:** **016–027** applied (2026-05-26). After **027**, run `npx tsx scripts/backfill-interim-calendar-2026.ts` on new DBs (idempotent). After **017**, run `npm run sync:ky:legislators` so `committee_memberships` populate from Open States `roles`.
- **legiscan_id verify loop** — After legislator sync or when member profiles show empty voting records: `npm run sync:ky:legislators` then `npm run diagnose:legislators` (exits 1 if active House/Senate rows missing `legiscan_id`). Optional district thumbnails: `npm run generate:district-thumbnails` (requires `NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN`; writes `public/geo/district-thumbs/`).
- [x] **Remove **`SENTRY_ENABLE_EXAMPLE_PAGE` from Vercel (and `.env.local` if set). ✅ Verified clear 2026-07-06 — not present in Vercel production env (`vercel env ls production`) nor in local `.env`/`.env.local`. Nothing to remove.
- **Legacy npm stacks** (puppeteer, GCS, pdf-parse, `three`, etc.) are **not** in root `package.json`. If you need them for a one-off script, use `docs/legacy-npm-deps/` and install into gitignored `optional/legacy-npm-deps/`.

## Recently completed

- **Security — cleared the residual dev/tooling `npm audit` advisories (2026-07-27, branch `claude/npm-audit-advisories-gie5x2`)** — Resolved the 9 high advisories deferred after the same-day prod-bundle pass. **9 → 0 high.** Two surgical changes, each re-verified against `tsc --noEmit` / `next lint` / `next build` (all clean): **(1) `adm-zip ^0.5.17 → ^0.6.0`** (fixes GHSA-xcpc-8h2w-3j85, the crafted-ZIP 4 GB-allocation DoS). Correction to the earlier deferral note: adm-zip is *not* only in gitignored scripts — it's imported by the tracked `src/lib/ky-legiscan-dataset-import.ts`, used by `sync:ky:dataset` (the weekly `legiscan-dataset-weekly.yml` GitHub Action) and `bulk-seed:ky`, but only as a **fallback** parser (primary is the native-zlib `iterateZipEntries`) and never in the Next.js prod bundle. The 0.5→0.6 breaking changes are confined to `extractEntryTo()` / utimes handling; the API this repo uses (`new AdmZip(buf)` / `.getEntries()` / `.entryName` / `.isDirectory` / `.getData()`) is unchanged — confirmed by an isolated round-trip smoke test. **(2) `overrides` brace-expansion pin retargeted `1.1.13` → `5.0.8` (blanket)**. Root cause: a newly-disclosed advisory (GHSA-mh99-v99m-4gvg / CVE-2026-14257, unbounded-expansion OOM) publishes its affected range as a flat `<= 5.0.7`, so npm audit flags **every** brace-expansion below 5.0.8 across all major lines — the old scoped `minimatch@3.1.5 → 1.1.13` pin (and even the 1.1.16 / 2.1.2 maintenance backports) can't clear it. A single blanket `"brace-expansion": "5.0.8"` override dedupes all three tree copies (was 1.1.13 via `@eslint/eslintrc`, 2.1.0 via `@typescript-eslint`, 5.0.7 via `react-email`→`glob`) to one patched 5.0.8; `next lint` passing confirms minimatch 3.x/9.x/10.x run against it. This *avoided* the whole-eslint-devtree bump that `npm audit fix` performs (historically nets +1); the lockfile actually **shrank** (−35 lines, dropping the now-orphaned `balanced-match@1.0.2` / `concat-map` copies). `@types/adm-zip@^0.5.8` left in place (0.6.0 bundles its own types but the two coexist without conflict — `tsc` clean; removable as future cleanup).
- **Repo housekeeping + non-breaking security patch (2026-07-27, branch `claude/repo-cleanup-docs-a1z8b4`)** — Full current-state verification: `tsc --noEmit`, `next lint`, and `next build` all clean; current-state doc links (README/TASKS/CLAUDE) resolve; CLAUDE.md org facts (`KYVKY_POSTAL_ADDRESS`, `alerts@`/`katie@` senders) match code; spot-checked shipped/deferred claims (Clear-all chips on the 5 browse surfaces, `SENTRY_ENABLE_EXAMPLE_PAGE` fully removed, `src/app/favicon.ico` gone) all hold. `npm audit` had drifted from the last-recorded 0 to 18 (newly-disclosed CVEs). Cleared the entire prod-facing bundle in two passes, each re-verified against `tsc`/`lint`/`next build`: **(1)** within-range patches — `axios ^1.10.0 → ^1.18.1` (patches 10 axios advisories incl. the prototype-pollution / DoS set), plus transitive `dompurify 3.4.12` / `fast-uri 3.1.4` / `tar 7.5.22` (a surgical 27-line lockfile change — the blanket `npm audit fix` churned 588 lines by dragging the eslint devtree and *netted +1*, so it was reverted); **(2)** `next 15.5.18 → 15.5.22` (clears the App-Router SSRF / DoS / cache-confusion advisories), `postcss 8.5.10 → 8.5.23` (source-map path-traversal; the `overrides` pin was the reason the Next bump alone didn't move it), and `sharp → 0.35.3` via `overrides` (libvips CVE-2026-33327/33328/35590/35591; Next's optional-dep range is `^0.34.3`, so an override was required — Node 22 satisfies sharp 0.35's engine floor; `next build` OG-image/icon generation confirmed working). **18 → 9 high.** The residual 9 are dev/tooling-only (adm-zip, eslint-tree brace-expansion) — **cleared to 0 the same day**, see the entry above.

- **Dictated UI fixes — masthead/footer, districts index, favicon, footer columns (2026-07-24, PR #201)** — Masthead + footer wordmark made non-selectable/non-draggable (`user-select:none` + `draggable={false}` in `Navigation.tsx` / `SiteFooter.tsx`). Design-system page side-menu logo dropped (single-file artifact — targeted string surgery + JSON re-validation; file 844→712 kB, orphaned base64 PNG removed). `/districts` index redesigned into a responsive House/Senate tile grid, every one of 138 tiles deep-linking to `/members/map?chamber=…&district=…` via a new `kyDistrictMapPath()` helper (detail pages retained, still in sitemap). Favicon refined to a **circle** in `#184D8F` (white ≈8.9:1), generated reproducibly by [`scripts/generate-favicons.py`](./scripts/generate-favicons.py) from `public/branding/mark-color-512.png`; removed the conflicting stock `src/app/favicon.ico`. Footer rebuilt into three labeled `nav` columns (Explore / Account / Learn more). Only the "Clear filters" item is deferred (see In Progress).

- **Front-end polish pass (2026-07-20, branch `claude/front-end-polish-bo645y`)** — Members-map selected-district value contrast + lighter boundary strokes; landing map preview → full `/members/map` link; primary-nav reorg with Meetings nested under a Committees dropdown; bill sponsor cards mirror the member-card stretch-link pattern; guides "Related pages" cleanup; generalized 4-stage bill progress meter (`ky-bill-progress.ts` → `BillProgressMeter` on cards / detail / digest email); `KY_SESSIONS` backfilled 2010–2024. The design-system v1.1 code migration landed here too — contrast promotions (`--success #15803D` / `--warning #B45309`), the `text.tertiary` metadata role, distinct party colors, back-compat alias retirement (Phase 4), and the radius reconciliation (Phase 3, zero visual change); `tsc` + `lint` clean. Deferred follow-ups tracked in In Progress.

- **Logged-out home page — topics copy + bill-highlight carousels (2026-07-18, branch `claude/home-page-logged-out-updates-1evwf5`)** — "Explore by topic" → **"Bills by topic"** with human-first labels sharing `LANDING_TOPICS` with the `/bills` quick-topic chips, plus an honest-sourcing subheading. Added a **"Most viewed bills"** carousel (PostHog uniq-visitors HogQL over `/bills/*`, cached 1h, falls back to cumulative `view_count` with a caption stating whichever metric ranked it) and a **"Recent legislative action"** carousel (substantive `legiscan_history` steps in the last 30 days, self-hiding when quiet), both via a shared `HomeBillCarousel` (scroll-snap, whole-card links, reduced-motion-aware, a11y-labeled). Operator PostHog-key follow-up tracked in In Progress.

- **AI plain-language bill summaries (beta) + quota-guard follow-ons — PR #103 et al. (2026-06-26 → 06-28)** — Revived `ky_bills.ai_summary`: a 2–3-sentence plain-language summary with a hedged "Who it may affect:" clause, grounded on title + description + topics + LegiScan subjects (zero LegiScan quota). Migration 034 (provenance / staleness), backfill script, beta frontend + "Tell us" CTA (on-site only, never in digests). Active session verified at **100% coverage** (1,737 / 1,737, 06-28); auto-summarize wired into the 6h bills sync (`--limit=300`, advisory / hash-gated). Same window: AI model id consolidated into `src/lib/anthropic-model.ts` + two dead `summarize.ts` modules removed; and the PR #102 quota-guard follow-ups all shipped — bill detail now renders 100% from the DB with zero live LegiScan (PRs #104/#105, migrations 035/036), mid-run `QuotaHoldError` classified `skipped`, and a weekly LegiScan Dataset Pull reconcile (`legiscan-dataset-weekly.yml`, 2026-07-15). Remaining: cross-bill audience lens pages (In Progress). Rationale: decisions.md §§ 2026-06-26 / 06-27 / 06-28.

- **Design + a11y follow-ups from the 2026-06-12 pass (shipped 2026-06-27 → 07-12)** — Auth-page a11y (distinct document titles per route, up-front password rules, shared `PasswordField` reveal toggle); search / glossary / committee a11y polish ("Suggested searches" rename, "500+" results count, in-page `GlossaryBrowser` filter, external-link "(opens in a new tab)"); `/search` relevance reorder demoting low-substance appointment / ceremonial resolutions on topical queries (`LOW_SUBSTANCE_RESOLUTION_PENALTY`, intent- and designation-guarded, demote-not-exclude); `/search` skeleton polish (2026-07-12). Only the signed-in mobile a11y sweep remains open (In Progress). Detail: decisions.md § 2026-06-27.

- **Members roster Phase 1 — payload trim, deep-link expansion, mount-cost trim, canonical slugs (2026-07-18)** — Executes Phase 1 of [docs/specs/members-roster-views.md](./docs/specs/members-roster-views.md) in four parts. **(1a — server-computed LRC link safety + active-only payload):** the roster cache build now runs `annotateKyLrcSeatConflicts` over the full deduped table (historical rows drive seat-turnover verdicts) and ships **active rows only**, conflicted rows carrying `lrc_district_link_unsafe: true` (flag omitted when safe — payload weight); `kyLegislatureProfileUrl`/`kyLegislaturePublicUrl` prefer the flag and keep the roster-scan fallback for the profile page's unannotated roster. Browse cards no longer receive the roster prop at all. Fixture A/B: API rows 143→140 (0 inactive), `/members` SSR HTML 568→545 kB, LRC link decisions byte-identical across scan/flag paths. **(1b — deep links past page 1):** `/members#slug` now auto-expands the owning section (`PaginatedSection` gained `expandToItem`) before the one-shot scroll — verified with a unique page-2 slug that cannot false-positive (card mounts, scrolls to 4233 px, typing afterwards does not hijack scroll). **(1c — mount cost):** `content-visibility: auto` + `contain-intrinsic-size` on browse card grid items, and `CopyableEmail` swaps its per-card MuiTooltip for a native `title` (nav-icon precedent). At 4× CPU throttle: typing max input event 144→96–104 ms (events >100 ms: 7→0–1), Senate toggle 728→512–624 ms, Load more ~[656,616,536,264]→[608,528,568,200] ms — the residual Load-more cost is React/Emotion mount work, which is the spec's deferred virtualization/lighter-card lever, not layout. **(1d — canonical `profile_slug`, migration 042):** name-derived slug, district-suffixed when the base collides across seats (`jane-smith-hd-5`), non-unique by design so same-person duplicate rows share one slug; backfill SQL mirrors `memberSlug()`; the legislator sync maintains it (probe query + skip when column absent). All explicit roster selects include it behind a 42703 retry-without fallback, so **deploy order vs. migration is free**. `/members` anchors, card hrefs, sitemap, and profile canonicals all use the stored slug via `memberCanonicalSlug`; legacy name-slug URLs and `#hash` aliases resolve via fallback matching and canonicalize to the suffixed URL (verified: 3 same-named fixture members get distinct hrefs, DOM ids unique, alias URL 200s with correct `rel=canonical`). **Verify rig committed** as `scripts/perf/` (`perf:members:stub` with `PROFILE_SLUG=1` post-042 mode + PostgREST-faithful 42703 errors, `perf:members:measure`) — see `scripts/perf/README.md`. **⚠️ Operator:** apply migration **042**, then `npm run sync:ky:legislators`.

- **/members interaction lag — per-keystroke O(cards × roster) work eliminated (2026-07-18)** — Typing in the roster search, chamber toggles, and "Load more" all janked hard (measured at 4× CPU throttle on a prod build: max input event **1.6 s**, 41 of 90 input events >100 ms, **6.7 s** of main-thread long tasks during ~4 s of typing). Three compounding causes: every `MemberCard` re-ran the LRC seat-conflict check **twice per render**, each a full-roster scan with regex name-normalization (`hasKyDistrictSeatDifferentPersonConflict`); nothing was memoized, so each keystroke re-rendered every mounted card; and the `#hash` deep-link effect re-fired on every render, smooth-scrolling the page back mid-typing (742 → user scrolls to top → hijacked back to 377 px). Fixes: **(1)** WeakMap-memoized seat→identity index in `ky-member-utils.ts` — conflict check is now O(1) per card with identical outputs (verified on engineered seat-turnover fixtures: stored + inferred `?DistrictNumber=` URLs still suppressed, non-conflict URLs untouched, byte-identical before/after); **(2)** `React.memo(MemberCard)` — stable `leg`/roster identities let unchanged cards skip re-render; **(3)** `useDeferredValue(searchQuery)` + `useMemo` filtering/section splits in `MembersBrowse` so keystrokes commit immediately and the grid catches up interruptibly; **(4)** hash-scroll effect fires once (ref-guarded), never on later filter renders; **(5)** browse/map roster select drops unused `committee_memberships` — `/members` SSR HTML 568 → 546 kB, `/api/roster/members` 90.6 → 69.8 kB. **After (same rig):** typing max input event 1576 → **184 ms**, events >100 ms 41 → **8**, typing long-task total 6.7 s → **0.46 s**; Load more max event ~0.8–1.5 s → 0.2–0.8 s; deep-link scroll preserved, no scroll-back while typing (377 → **0 px**). **Verify recipe (no Supabase creds needed):** local PostgREST stub serving `ky_legislators` (140 active + 15 historical rows incl. same-seat turnovers) + `ky_sources` on :54321, `.env.local` pointing `NEXT_PUBLIC_SUPABASE_URL` at it, `next build && next start`, Playwright + CDP `Emulation.setCPUThrottlingRate` driving search/toggles/Load more with `PerformanceObserver` (`event` + `longtask`) — run against a baseline worktree build for the A/B.

- **Bill status: "Vetoed" mislabel for bills that became law (2026-07-17, PR #175)** — Root-caused from an accuracy spot-check that flagged SB197 2026RS showing "Vetoed" despite enacting as Acts Ch. 202. The deeper bug: `mapLegiScanBillStatus` keyed "became law" off the literal substring `"acts ch"`, so it missed KY's comma citation `"Acts, ch. 194"` and ignored the legal fact that a **line-item veto still enacts the rest of the bill** (Ky. Const. §88) — 7 more bills carried the same mislabel. Fix grounds detection in KY law: `ACTS_CHAPTER_RE = /acts\s*,?\s*ch/` (all punctuation variants, on `last_action` **and** history) plus a `legiscanActionIndicatesLineItemVeto` became-law → `Chaptered` signal even when no chapter number is recorded. Only a **full veto that is never overridden** stays `Vetoed`. Also hardened the two mapper call sites that had `history` available but dropped it (`ky-legiscan-dataset-import.ts` re-import + `refresh:bill-status` drift tool) so a re-import can't revert history-only corrections; the `getMasterList` summary paths have no history by construction and keep relying on the change-hash detail sync + backfill as backstop. Regression suite 14 → **22 cases**. **Backfill applied to primary:** 39 rows — 10 `Vetoed`→became-law (9 `Chaptered`, 1 `Veto Override`) + 29 budget bills `Chaptered`→`Veto Override`. The accuracy audit needs no change (shares the mapper, already passes history). Rationale: **decisions.md § 2026-07-17 — Bill status mapper: became-law detection grounded in KY law**.

- **Hierarchical committee agendas + 2025 backfill (2026-07-15, PR #166 — merged)** — Two-in-one: fix the flat agenda rendering that hid the LRC source's parent/child structure, and backfill the 2025 committee-meetings gap. **Rendering:** new `AgendaLine` + recursive `AgendaNodeList` shared by `MeetingAgendaBlock` / `AgendaSearchResults` / `BillHearingsSection` — numbered `ol` at the top level, disc/circle `ul`s beneath; only the bill designation is inline-linked (drops the whole-line underline and the redundant `BillNumber` chip that duplicated the linked designation). Legacy `•`-fold fallback preserved for pre-depth meetings. **Scraper + schema:** `lrcLegislativeCalendarParser` now counts leading `\t`s before whitespace collapse to recover depth; migration **039** adds `ky_committee_agenda_items.depth SMALLINT NOT NULL DEFAULT 0`; the LRC sync writes `depth` on insert. **Bill resolution (interim references):** `lrc-bill-reference-parser` recognizes LRC shorthand (`25RS SB 15`, `24SS HB 2`, `22EX HB 1`); `inferSessionLabelFromMeetingDate()` fills bare mentions (`SB 58: …`) so interim-review lines to enacted bills actually resolve to `ky_bills`. **Backfill (via existing `backfill-lrc-calendar-wayback`):** 2025 corpus — **95 meetings across 66 committees, 935 agenda rows, 94 with `member_refs`, 70% with depth, 93/93 non-BR bill mentions resolved** (5 unlinked are BR pre-file drafts, which `ky_bills` doesn't track). 2026 corpus — 46 of 87 meetings recovered full hierarchy; the Apr 15 – Jul 8 window has zero Wayback captures so those 38 meetings stay on the legacy fallback (still cleaner than before). Weekly sync captures depth + session automatically going forward — no new cron. Rationale: **decisions.md § 2026-07-15 — Hierarchical committee agendas**.

- **Sentry SDK deferral — ~82 kB off every route's First Load JS (2026-07-15, PR #167 — merged)** — Closes the real bundle-size lever the 07-15 shell-bundle-trim investigation surfaced (see the PRs #120/#121 entry below). `Sentry.init()` was already on `requestIdleCallback`, but the SDK **module** bundled upfront because `import * as Sentry from '@sentry/nextjs'` ran at module load in `instrumentation-client.ts` and `Sentry.captureRouterTransitionStart` was re-exported synchronously for Next's App Router hook. Switched to a dynamic `import("@sentry/nextjs")` inside the same idle callback so the SDK moves to a lazy chunk that loads after LCP; exported `onRouterTransitionStart` as a **proxy** that buffers `(href, navigationType)` tuples in a bounded ring (cap 32) until the real handler resolves, then flushes and swaps to a direct delegate. Measured deltas via `next build`: shared-by-all **259 kB → 175 kB (−84 kB, −32%)**; every route drops ~82 kB (`/` 435→353, `/bills` 484→402, `/feed` 489→412, `/search` 469→387, `/members` 450→368, `/committees` 434→352, API routes 259→176). Network waterfall from `next start` confirms Sentry now on lazy `chunks/2367.js` (131 kB gz), initiated at ms=137 by webpack dynamic import, not preloaded. Smoke-tested: SDK v10.63.0 loads post-idle, multiple client navigations succeed with no proxy-shim console errors. **Tradeoff carried over from the prior deferral:** errors thrown in the first ~1–2 s before idle fires aren't captured by Sentry — unchanged from before this PR; browser default error handling still logs them.

- **Agenda previews on committee browse cards + `Event`/`FAQPage` JSON-LD (2026-07-15, PR #162 — merged)** — Cleared three deferred frontend items in a single pass. **Agenda previews on `/committees` browse cards** — `fetchKyCommitteesBrowseEnriched` now batches one `IN (nextMeetingIds)` query against `ky_committee_agenda_items` and attaches up to 3 normalized preview lines per card (`KYCommitteeAgendaPreviewLine`); `KYCommitteeCard` renders them as a tight disc list under the "Next meeting" footer, one-line truncated. Closes the "agenda previews on browse cards" half of the 2026-07-04 UX-pass deferred set (portraits half shipped in `22e94ea`). **`Event` JSON-LD on `/meetings`** — new `buildMeetingEventJsonLd()` in `src/lib/structured-data.ts` parses the leading `"H:MM am|pm"` from `time_and_location`, resolves the `America/New_York` offset with `Intl.DateTimeFormat` (`-04:00` in EDT / `-05:00` in EST — so summer/winter DST is correct on future dates), emits one Event per non-cancelled upcoming meeting (`eventStatus`, `eventAttendanceMode: OfflineEventAttendanceMode`, `organizer` GovernmentOrganization, `location` Place when parseable) capped at 40 nodes. Cancelled meetings and rows without parseable times degrade cleanly (fall back to bare `meeting_date`). Emits alongside a `BreadcrumbList`; verified end-to-end at `/meetings` — 40/40 events in the SSR HTML with correct EDT offsets and location = `"Annex Room 149"` etc. **`FAQPage` JSON-LD on `/glossary`** — new `buildGlossaryFaqJsonLd()`; each glossary term becomes a `Question` (name = term title as displayed, matching Google's "match visible content" guidance) with the plain-text definition as `acceptedAnswer.text`; verified 76/76 questions render (matches the on-page "N of 76 terms" count). Rich-result surface is narrow post-2023, but the markup remains valid for generative engines. Closes both JSON-LD halves of the 2026-06-21 SEO/GEO pass's deferred set; `next/image` adoption sweep already shipped in the 2026-07-03 perf passes. tsc + eslint clean; verified in-browser at `/committees`, `/meetings`, `/glossary`.

- **Removed dead `VotingRecord` component + `voteExplanations` export (2026-07-06)** — Salvaged from an abandoned uncommitted change found in the stale `gifted-williams` worktree during admin cleanup (same dead-code family as the 07-05 `LegislativeProcess` removal). `src/components/ui/VotingRecord.tsx` (163 lines) and the `voteExplanations` record in `src/lib/tooltipContent.ts`: zero importers repo-wide, and the vote-explanation copy duplicated (less accurately) what `voteCountTooltips` already covers under the accuracy audit's glossary pass. tsc + eslint clean. Stale `claude/*` worktrees + local branches pruned in the same session.

- **Removed dead `LegislativeProcess` component (2026-07-05)** — Deleted `src/app/components/LegislativeProcess.tsx`: zero importers repo-wide (no static/dynamic imports, no re-exports, no barrel), and it hardcoded U.S.-Congress process terminology ("Markup", "Bill introduced to Congress", "Committee markup session") that doesn't apply to the Kentucky General Assembly — exactly the kind of leak the weekly accuracy audit's LLM pass flags. tsc + eslint clean after removal.

- **Committee record merge round 2 — seed rows that duplicated after the June merge (2026-07-04)** — The UX-pass data finding ("Commission on Race **&** Access" vs "…**and** Access") turned out to be **10 pairs**, all migration-027 seed rows (short types `IJ`/`S`, PDF-backfilled future meetings) whose committees first appeared on the live LRC calendar *after* the 2026-06-12 merge, minting full-label twins on 2026-06-13/20/27. Not a guard evasion: `normalizeCommitteeNameForDupes` already folds `&`→"and", and the weekly audit's same-rsn warn had flagged every pair since 2026-06-14 (unactioned; the 6/21 + 6/28 audit-run failures muffled it). **Applied to primary 2026-07-04:** `merge:duplicate-committees --live` auto mode, 10 pairs, 166 actions, `ky_committees` 66 → 56; survivors = full-label rows (what current calendar syncs upsert — verified via meeting `source_url`/`updated_at` before merging); loser slugs → `aliases` (308s verified); Race card now shows "Next meeting · Mon, Jul 20, 2026". No code changes. Rationale: **decisions.md § 2026-07-04 — Committee record merge, round 2**.

- **Meetings + committees UX pass (2026-07-04, branch `feat/meetings-committees-uiux`)** — First dedicated UX (vs a11y) pass on `/meetings` + `/committees`. `/meetings` list: day-group headers with Today/Tomorrow prefixes (cards drop the repeated date row via new `CommitteeMeetingCard.hideDate`, also used in the calendar day panel), cancelled meetings dimmed with "(cancelled)" aria suffix, Recent range now newest-first. Committee detail: meetings split **Upcoming** (soonest first — the next meeting was buried under Nov/Oct interim dates) / **Past** (newest first), next meeting chipped + agenda pre-expanded, per-meeting `#meeting-<id>` anchors with hash auto-expand; meeting cards sitewide deep-link to them ("View meeting details"); quick-facts "View agenda →" targets the next meeting directly. `/committees`: cards gain `Next meeting · <date>` footer (no new queries — computed in `fetchKyCommitteesBrowseEnriched`) and subcommittees sort after full committees (Budget Review wall no longer buries major committees). Detail header decluttered (redundant back button, duplicate materials link, stretched overview card). **Deferred (both since shipped):** ~~agenda previews on browse cards~~ (2026-07-15, see top of Recently completed); ~~portraits for committee-card leadership avatars~~ (2026-07-14, commit `22e94ea`). **Data finding spawned as separate task:** "Commission on Race & Access" vs "…and Access" near-dupe rows (evades the &/and-blind near-dupe guard). Rationale: **decisions.md § 2026-07-04**.

- **Performance passes 1 + 2 (2026-07-03, PRs #120 + #121 — merged 2026-07-04)** — First dedicated speed work. Root causes found by local-prod-build Lighthouse baseline: home SSR HTML was a spinner (auth-gated hero) and `/members` shipped ~31MB of original-resolution images (18.3MB hot-linked governor portrait). **PR #120:** marketing hero server-rendered (brief hero swap for signed-in visitors — approved trade); district thumbnails + legislator portraits via `next/image` (`images.remotePatterns` allowlist for state hosts, tail hosts keep plain `<img>`, optimizer→img→initials fallback); hero 91KB jpg→28KB webp + head preload; explicit `revalidate=60` on `/`; `s-maxage` on `/api/search`; `dayjs` removed. **PR #121:** landing map preview mounts only near-viewport (was eagerly fetching the 462KB mapbox chunk + 1.9MB GeoJSON per home visit); district GeoJSON simplified 64% (PIP parity validated — 0.076% boundary-only flips); Typekit non-blocking **with metric-adjusted Georgia fallback face** (`size-adjust` 83.1%/92.8%, canvas-measured — shipping async fonts without it sent detail CLS to 0.65); browse `loading.tsx` skeletons scoped via `(browse)` route groups. **End state vs baseline:** home 68→98 desktop / 30→74 mobile, all pages ≥74 mobile, FCP ~1s mobile sitewide, CLS ≤0.031, `/members` transfer 31MB→1MB. **Deferred lever:** shared-bundle size (~250KB gz — originally attributed to MUI/emotion). ~~MUI/emotion trim~~ **investigated 2026-07-15** (`perf/shell-bundle-trim`): First Load JS unchanged on every route. Attribution was wrong — the 259 kB "shared by all" chunk is chunk `5857` (~130 kB gz) which is **Sentry SDK + Next.js router runtime**, not MUI. MUI/emotion lives in per-route vendor chunks (`9426`, `2362`, `2013`); `optimizePackageImports` already tree-shakes them. Same-day pass shipped a small hygiene fix (dead `GlobalSearchBar` in `Navigation.tsx` removed, `MuiTooltip` on desktop nav icons swapped for native `title` — layout chunk 40 kB → 26 kB uncompressed, but net FirstLoadJS = 0 kB because the removed components moved to shared vendor chunks that other routes already load). ~~**Real lever = Sentry SDK deferral**~~ **shipped 2026-07-15 as PR #167** — dynamic `import()` + proxy `onRouterTransitionStart` with a bounded buffer; landed **−84 kB (−32%) off shared-by-all** and **~82 kB off every route's First Load JS**. Full record: top of Recently completed. Rationale: **decisions.md § 2026-07-03** (passes 1 + 2).

- **LegiScan quota guard at client + edge-triggered Slack alerts (2026-06-26, PR #102)** — Monthly LegiScan quota was climbing while `bills` sync sat on hold the whole window, masked by redundant Slack flags (19× threshold posts / 48 h). Shipped: `LegiscanQuotaHoldError` guard inside `KyLegiScanClient.request()` (every client method respects the hold, 60 s TTL), DB-only soft fallback in `getCachedLegiscanBillDetail`, interim gate on `syncKyVotes` + `syncKyLegislatorBios` (`force` / `KY_SYNC_FORCE_INTERIM` overrides), edge-triggered banded quota alerts (`90/95/98/100`, posts only on band rise / month rollover), and quieter sync digests (`SLACK_SYNC_BILLS_DIGEST_ALWAYS=false`; quota-hold skips no longer count as interesting). Three engineering follow-ups remain open (persist roll-call to DB; classify mid-run quota-hold as `skipped`; Dataset Pull weekly reconcile) — see **In Progress**. Rationale: **decisions.md § 2026-06-26 — LegiScan quota guard**.
- **SEO + GEO discoverability pass (2026-06-21, PR #98)** — First indexability + generative-engine pass on `kyvky.com`. Sitemap goes from **9 → ~1,200 URLs**: dynamic bills (active session, capped at 5,000), members, committees via new `src/lib/sitemap-data.ts`; `src/app/sitemap.ts` is now async with `revalidate = 3600`. Static paths gained `/about`, `/glossary`, `/privacy`, `/terms`. **schema.org JSON-LD** via `src/lib/structured-data.ts` + `src/components/seo/JsonLd.tsx`: root emits `Organization` + `WebSite` (with `SearchAction`) via `@graph`; bill detail emits `Legislation` (sponsors as `Person[]`, KY jurisdiction, dates) + `BreadcrumbList`; member detail emits `Person` (jobTitle, `worksFor` KY GA, `memberOf` PoliticalParty, `sameAs` Ballotpedia) + `BreadcrumbList`; committee detail emits `GovernmentOrganization` (parent KY GA) + `BreadcrumbList`. **OG image**: dynamic edge `ImageResponse` at `src/app/opengraph-image.tsx` (1200×630 branded card) replaces the broken `og-image.jpg` reference — Next auto-injects via file-based metadata, so explicit `images:` blocks were removed from `layout.tsx`. **`llms.txt`**: dynamic route handler at `src/app/llms.txt/route.ts` (1 h revalidate, 24 h SWR) lists sections, data sources, and citation guidance. **Per-page canonicals + `openGraph.type`** on all three detail routes. **GSC verification code** populated (replaces `"your-google-verification-code"` placeholder). **Deferred (all since shipped):** ~~`Event` JSON-LD on `/meetings`~~ (2026-07-15, see top of Recently completed); ~~`FAQPage` on `/glossary`~~ (2026-07-15, same sweep); ~~`next/image` adoption sweep~~ (shipped in the 2026-07-03 perf passes). **Operator follow-ups:** verify ownership in GSC and submit `sitemap.xml`; same flow for Bing Webmaster Tools (import from GSC). Rationale: **decisions.md § 2026-06-21 — SEO + GEO discoverability pass**.
- **Committee material link-status — 404 detection persisted (2026-06-13)** — LRC document URLs die after a session ends; the materials section linked straight to the 404. Now: migration **031** adds `ky_committee_materials.link_status` (`ok`/`dead`) + `link_checked_at`; a shared probe lib (`ky-committee-material-link-probe.ts`) backs both the weekly `audit:accuracy` link probe (now persists results for its sample when `ACCURACY_PROBE_LINKS=true`) and a new full-coverage `npm run probe:committee-links` (`:dry`; `--only-unchecked`/`--limit`/`--committee`/`--concurrency`). Only definitive outcomes recorded (404→dead, 2xx/3xx→ok; transient/403/5xx leave the value untouched). Committee detail flags a `dead` material as line-through + "Link unavailable" chip instead of a broken link. **⚠️ Apply migration 031 to primary before deploy** (read query selects `link_status`), then run `probe:committee-links` to backfill. Typecheck + eslint clean. Rationale: decisions.md § 2026-06-13 — Committee material link-status.
- **Design + a11y pass — never-reviewed surfaces (2026-06-12, PR #88)** — Combined `/design-critique` + `/accessibility-review` (WCAG 2.1 AA) of live `kyvky.com` at ~1280px + ~500px (xs active), covering the surfaces the 2026-06-01 pass skipped: `/committees`, committee detail, `/meetings`, `/search`, `/glossary`, `/about`, signed-out auth. **Fixed:** mobile nav drawer gains Log in + Search (header had hidden both at xs); decorative signed-out committee-card bookmark removed (fake affordance) + interactive toggle 44px floor (`KYCommitteeCard` + `CommitteeMeetingCard`); `/search` + `/meetings` inputs get real labels; all four `/search` filter Selects get `labelId`; `/search` live region for result counts + `<h2>` results header; `/search` xs filter layout fix; sub-44px targets raised (`GaChamberFilterBar` pills, Upcoming/Recent toggle, subject chips); committee-detail meeting-materials heading order; clock icon replaces location pin next to meeting times. **Systemic lesson:** every major finding was a recurrence of a 2026-06-01 failure class — per-call-site fixes don't stick; prefer theme/component-level enforcement. **Open remainder** (committee-dedupe re-land, signed-in sweep, Minor polish) → In Progress. Rationale: decisions.md § 2026-06-12 — Design + a11y pass.
- **Committee record merge — seed-vs-LRC duplicates (2026-06-12, PR #89; re-landed on `main` 2026-06-13)** — Root cause: LRC switched the calendar `CommitteeType` URL param from short codes (`IJ`/`S`, also used by migration 027's seed) to full labels, so the `(lrc_rsn, committee_type)` upsert minted a second row per interim/statutory committee — **14 pairs**, data split across rows. Shipped: `diagnose:committee-duplicates`, `merge:duplicate-committees` (idempotent, dry-run default, date-only meeting-collision handling, follows/materials/events re-point, loser slug → survivor `aliases`), migration **030** (`ky_committees.aliases` + GIN), alias 308-redirect on `/committees/[slug]`, near-dupe warn in `audit:accuracy` checkCommittees. **Applied to primary 2026-06-12: 14 pairs, 244 actions, `ky_committees` 69 → 55.** **Re-land note:** PR #89 was mistakenly merged into the #88 branch, not `main`, so the code missed `main`; re-landed 2026-06-13 in **PR #90** (migration 030 is idempotent — already applied to primary). Rationale: decisions.md § 2026-06-12 — Committee record merge.
- **District-map hover UX (2026-06-11, PRs #86/#87)** — Replaced the non-interactive full-card map popup on `/members/map` with a chromeless hover chip + sidebar member preview, plus selected-fill tint and smoother transitions. Rationale: decisions.md § 2026-06-11 — District-map hover UX.
- **Sine-die display override for pending bills (2026-06-10, PR #85)** — Pure display-layer fix (no DB sync): pending bills (`In Committee`/`Introduced`/etc.) from a session that has adjourned sine die now show **"Adjourned Sine Die"** instead of their stale pre-adjournment status. `sessionHasEnded()` (returns false for unknown sessions) + `isActivePendingBillStatus()` gate the override in `BillStatusMetaChip`; terminal statuses and active/future sessions untouched. Closes accuracy finding HB563. Rationale: decisions.md § 2026-06-10 — Sine-die display override.
- **Phase 5b — LRC enrollment-actions sync + accuracy backfill pass (2026-06-11)** — Shipped `sync:lrc:enrollment-actions` (parser + sync + Vercel cron 14:45 UTC). Writes governor sign/veto/override history from `enrollment_actions.html` with action-dated `observed_at`. Primary backfill: **466** history rows. Same session: topic classifier precision pass (`drug` → specific healthcare phrasing; `financial literacy`, body-worn camera / search-warrant / electrical-shock keywords) + `npm run topics:reclassify` (**2,392** bills changed); NV/absent glossary scoped to final-passage (third reading) vs procedural votes; `npm run refresh:bill-status` for post-mapper LegiScan drift; `remap-recommittal-statuses` fixed (no `status_code` column); committee materials historical backfill (**12** inserted, **1,002** updated). **Not in scope:** meetings/agenda rows (still `sync-lrc-calendar.yml` GH Actions) — enrollment actions are bill executive history only. Rationale: **decisions.md § 2026-06-11**.
- **Committee membership reconciliation + recommittal mapper bug (2026-06-09, PR #84)** — Four issues addressed: (1) `mapLegiScanBillStatus` now correctly returns `In Committee` for recommitted bills (`/recommit/i` guard bypasses the `CHAMBER_PASSED_CODES` protection — recommittals are genuine backward steps); `scripts/remap-recommittal-statuses.ts` corrects existing rows. (2) `syncKyLegislators` pre-fetches `ky_committees` and writes canonical `ky_committees.slug` values instead of raw Open States-normalized slugs, eliminating substring-matching fragility. (3) `fetchCommitteeAssignmentsForLegislator` inverted to LRC-calendar-primary / OS-fallback so Chair/Vice Chair role labels appear on member profiles. (4) `checkLegislators` accuracy checker now diffs `committee_memberships` against OS roles (warn-only, guarded on OS returning non-empty roles). New `npm run reconcile:committee-memberships` diagnostic. Rationale: **decisions.md § 2026-06-09 — Committee membership reconciliation**.
- **Security — /api/intelligence LLM hardening (2026-06-08, PR #80)** — Fixed `getClientIp` XFF spoofability: `x-real-ip` first (Vercel edge, not client-forgeable), then last `x-forwarded-for` entry (Vercel appends the real client IP at the end). Added global daily Anthropic ceiling via existing Supabase token bucket (`anthropic:llm:daily`, capacity=200, refillPerSec=200/86400) before `generateWhyItMatters`; when hit, items return without `whyItMatters` (graceful degradation, not 429). Worst-case ~$0.60/day at Sonnet pricing. Signed-in-only considered and declined. Rationale: **decisions.md § 2026-06-08 — /api/intelligence LLM call security**.
- **Security — remove hardcoded Sentry DSN (2026-06-08, PR #79)** — Removed DSN fallback literals from `sentry.server.config.ts` and `sentry.edge.config.ts`; `enabled: !!dsn` so Sentry stays silent when neither `SENTRY_DSN` nor `NEXT_PUBLIC_SENTRY_DSN` is set. Rationale: **decisions.md § 2026-06-08 — Sentry DSN: no hardcoded fallback**.
- **PostHog — named user-action events (2026-06-08, PR #78)** — Stable client-side events in `src/lib/analytics.ts` for PostHog Action subscriptions: `user_registered` (both register paths, tagged `needs_verification`), `bill_followed` / `bill_unfollowed` (after API success only), `preferences_saved` (digest + event-types save; per-topic toggles skipped), `account_deleted` (before signOut). Rationale: **decisions.md § 2026-06-08 — PostHog named user-action events**.
- **Wave 3 — Session-record spike (2026-06-02)** — Inspected `apps.legislature.ky.gov/record/26rs/record.html` and the most promising child pages to identify what gap-fills LegiScan. **Outcome:** `enrollment_actions.html` is the standout — date-stamped governor actions with line-item-veto distinction, not reliably in LegiScan. Recommend a Phase 5b parser + `sync:lrc:enrollment-actions` writing `ky_bill_status_history` rows with `observed_at = action_date` (not `now()`). Roll-call PDFs (`convening_*.html`, `committee_votes_*.html`) deferred — PDF extraction is a separate project. Per-status bill-list pages (`vetoed.html`, `enrolled.html`, `law.html`) skipped — redundant with `/bills?status=…`. Full report: **[docs/specs/session-record-spike-report.md](./docs/specs/session-record-spike-report.md)**. New fixture: `fixtures/lrc/legislative-record-enrollment-actions-26rs-live.html`.
- **Remove legacy event-database stack (2026-06-02, PRs #57 + #58)** — Tracker entry backfill for the cleanup that landed on `main` 2026-06-02. PR #57 removed `/events`, `/events/[id]`, `BackNavigation`. PR #58 removed the remaining cascade: `/activity`, `/explore`, `/link-dashboard`, `/live-content`, `/table`, plus `EventPreviewModal`, `SearchResultCard`, `TableView`, `LinkedEventsDisplay`, `LinkedBillsDisplay`, `EventDetailVerification`, `VideoGraphVisualization`, `StatusBar`, `PoliticalIntelligenceCard`, `BillListWithRefresh`, and the legacy libs `useBillsData`, `useEventBillLinks`, `billStages`, `calendar-events`. Net: −10,196 lines across the two PRs.
- **Follow committees v1.5 (2026-06-02)** — Closed the four v1.5 gaps from PR #38: `agenda_updated` + `meeting_cancelled` events in LRC sync (migration **028** relaxes dedupe index to include `agenda_content_hash`), `/meetings?follows=me` filter (mirror of `/bills?follows=me` — signed-in toolbar toggle, deletable chip, login alert, follow-but-empty empty state), committee events in `GET /api/me/activity` (new `committee_event` kind alongside `bill_event`/`hearing` in the unified timeline + `committee` filter chip on `ProfileActivitySection`), follow toggle on `/meetings` rows + agenda search results (lucide/MUI bookmark IconButton). Three new digest event slugs (`committee_meeting_scheduled` / `committee_agenda_updated` / `committee_meeting_cancelled`) wired through `KY_DIGEST_EVENT_GROUPS.committee_interim`. **Design refinement same day:** activity rows on both `LandingPersonalStrip` and `ProfileActivitySection` adopt the bill-card pattern via a new shared `ActivityStatusChip` — tone-mapped status chip (`kyDigestEventChipTone`) with green ✓ on success / red ✗ on error, `body1`/500 bill or committee title, clickable committee names. Bug fix: committee_event rows in `ProfileActivitySection` no longer render the label twice. CTA on the strip points to `/profile#activity` ("View all activity") — the unified timeline, not `/feed` (bills-only). Rationale: **decisions.md § 2026-06-02 — Follow committees v1.5** + **§ 2026-06-02 — Activity timeline visual treatment**.
- **Mobile design + a11y pass (2026-06-01, PRs #45 + #46)** — Combined `/design-critique` + `/accessibility-review` (WCAG 2.1 AA) sweep of the five core read paths on live `kyvky.com` at 375/390px viewports. Scope: `/`, `/bills`, `/bills/[id]`, `/members`, `/members/map` (signed-out read paths only — profile/feed/follow flows deferred). All 10 top fixes shipped: theme-level target-size pass (`theme.ts` `MuiIconButton` root 44×44, `MuiSelect.select` minHeight 44); copy-email IconButtons → 44×44; topic chips on home → 44px at `xs`; address input on `/members/map` got a real `<label>`; `/bills` filter dropdowns audited (`InputLabel labelId` already present, target-size covered by theme override); duplicate `<h1>Bills</h1>` ruled a false positive (Suspense fallback is `null`); `/bills/[id]` heading order repaired (`component="h2"` on `variant="h6"` section labels); hero CTAs confirmed 44px (no override needed); Mapbox zoom controls scoped to 44×44 at `xs`; chamber filter pills on `/members` inherit theme floor; hero subtitle contrast verified 7.7:1 via scrim math (passes AAA). Rationale: **decisions.md § 2026-06-01**. **Deferred:** real-device pass at 375px DevTools mobile emulation, plus profile/feed/follow flows (next mobile sweep).
- **Functional-test fixes + UI design-system normalization (2026-05-27, PR #40)** — 8 functional-test fixes shipped + verified on localhost (LRC accuracy full match). Same PR landed the cross-surface design-pattern consolidation: shared `CardGrid`/`CardGridItem` + `GRID`/`FOCUS_RING`/`INTERACTION` tokens (`src/lib/ui-tokens.ts`); normalized member identity (`legislatorAvatarDescriptor`, single `avatarInitialsFromName`, `LegislatorIdentityBlock` `meta` slot); Governor's office folded into the standard `/members` grid; unified bill primary/co-sponsor cards (`SponsorCard`); educational tooltips no longer render a "Learn more" link and no longer linger (surface reverted to non-interactive); member profile redesign (2-col top, **Official profile (KY Legislature)** link, filterable 3-col sponsored bills with show/hide co-sponsored); committee detail redesign (2-col top + quick-facts panel with Next-meeting callout + jump links, members as a `MemberCompactCard` grid). Rationale: **decisions.md § 2026-05-26 — UI design-system normalization pass**.
- **Session milestones + interim banner (2026-06-01)** — Wave 3 sub-item. `ky-sessions.ts` gains optional `milestones` (`vetoRecessStart`, `vetoRecessEnd`, `sineDie`) on each `KYSessionRecord`, plus `getInterimPeriod()` and `getSessionPhase()` helpers. `getSessionBannerModel()` now returns a `phase` (`in_session` | `veto_recess` | `final_days` | `interim`) and a `contextLine`; `SessionBannerServer` renders the phase-aware second line so today (in interim) reads **"2026 Interim: April 16, 2026 – next session convenes"** with an interim-committees explainer instead of the previous generic after-session caption. New glossary entries: `concurrence`, `veto_recess`, `interim_period`. 2026 RS `milestones` field is intentionally left undefined (TODO comment in `KY_SESSIONS`) until the LRC-published veto-recess dates are confirmed; banner falls back to the clean session range when no milestones are set. Rationale: **decisions.md § 2026-06-01 — Session milestones + interim banner**.
- **Voice & tone follow-up pass (2026-05-27)** — Cleared the open follow-ups from the audit below. Expanded **Legislative Research Commission (LRC)** on first use on `/meetings`, `/committees`, committee detail, member profile, and the Frankfort resources page (home banner + profile notifications already done). Confirmed `**/find-content`** is an unlinked internal tool — left as-is. Fixed a member-profile empty state that leaked an internal *"run a bills sync"* instruction → honest caveat (*"Sponsor data may lag the official record"*); reworded the resources page's internal *"Hearing scheduled"* token to plain language. **Deprecated legacy `/events*`* (reads the old `ky_meetings` table; superseded by `/meetings` + `/committees`): added `events/layout.tsx` `noIndexMetadata`, dropped `/events` from the sitemap (added `/meetings` + `/committees`), removed its `BackNavigation` fallbacks → still serves by direct URL but out of search + in-app nav. Sweep otherwise clean (terms, privacy, glossary, licenses, search, feed, dashboard on-voice). Typecheck + localhost spot-check clean.
- **Voice & tone audit + sitewide copy pass (2026-05-27)** — Audited live kyvky.com + emails. Broadened the email voice guide into a sitewide one: `docs/email-voice-and-tone.md` → `**docs/voice-and-tone.md`** (added **Honest sourcing** + **Warmth through anticipation** principles, an explicit non-partisan rule, a reference-vs-marketing register note, and naming/heading/auth conventions). Copy fixes: standardized the auth verb to **Log in** (nav, footer, login page, profile gate, follow labels); collapsed five variants of the district-map feature name to one — **Find my legislators** (nav, map H1 + tab title, both hero CTAs, followed-bills empty state); **fixed a live broken link** — `/district-map` 404 → `/members/map` (welcome-email CTA + followed-bills empty state); device-neutral **"Select"** (was "Tap"); **"Bills"** heading + tab title (was "Explore Bills"); **"Load more"** (was "Load more bills"); **"141 members"** (was "people"); expanded **Legislative Research Commission (LRC)** on the home session banner; de-jargoned About data sources ("LegiScan", dropped "GeoJSON/geocoding") and the notifications helper (dropped internal "our LRC calendar sync"); welcome-email topic card now states the automated-tagging caveat; home `<title>` + meta description de-abbreviated (**Track Kentucky legislation**, "representatives" not "reps"). Spot-checked on localhost; typecheck clean; staged on `feat/home-returning-hero-personalization`. New canon: `**docs/voice-and-tone.md`**.
  - **Follow-up status:** LRC first-use expansion, `/find-content`, and the full guide sweep are **done** (see "Voice & tone follow-up pass" above). Legacy `/events` was **deprecated** rather than copy-polished.
    - **Verification gap — closed (2026-05-27):** logged-in browser pass confirmed the followed-bills empty state ("Select Follow…" + "Find my legislators" → /members/map), the notifications LRC expansion, and "Log in" across nav/login/footer. The pass also caught **5 more device-specific verbs** the sweep missed (followed-committees empty state, profile activity, bills "Following" empty state, district map ×2, privacy "Click Unsubscribe") — all changed to "Select"/"select".
    - **Intentionally kept:** About's "no AI-generated summaries in digest emails" and the welcome-email card heading "Find your legislators" (second person in a heading, vs. the first-person "Find my legislators" button) — both deliberate, not oversights.
- **TASKS + backlog refresh (2026-05-26)** — Reconciled tracker with PR **#38** (committee follows), PR **#37** (meetings browse window), home Lottie ship, migrations **026–027** on primary.
- **Home hero — capitol background + Lottie icons (2026-05-25)** — Optimized `ky-capitol-hero.jpg`; `HoverLottie` on landing feature cards; `LandingHeroCtas` / `landingHeroStyles` refactor (`37c881a`).
- **Follow committees v1 (2026-05-22, PR #38)** — Migration **026** (`ky_committee_follows`, `ky_committee_events`); `GET/POST/DELETE /api/committees/[id]/follow`; `FollowCommitteeButton` + `useFollowedCommittees`; profile **Followed committees** section; digest **Committee meeting scheduled** block; LRC sync emits `meeting_scheduled` on new meetings; migration **027** interim committee seed + `scripts/backfill-interim-calendar-2026.ts`; agenda search bill links; `preview:digest` committee preview support.
- **Meetings browse — session window (2026-05-22, PR #37)** — `/meetings` browse widened to session start so historical in-session meetings surface.
- **Committee + member UI polish (2026-05-22, UI-8–UI-11)** — Committee kind tags (`CommitteeKindChip`, `resolveKyCommitteeKind`) on meetings browse, committee cards/detail, member profile tiles; `OfficialSourceLinks` replaces heavy outbound button stacks; member roster two-line title + district; committee detail merges “At a glance” into overview + subcommittee parent link via `resolveKyCommitteeParent()`.
- **UX quick wins + browser review fixes (2026-05-22)** — Profile hash scroll + sticky section nav; map empty vs no-district states; member profile section reorder (Sponsored → Voting → Committees) with `KYBillCard` gallery, committee assignment tiles (`shortKyCommitteeLabel`), static district thumbnails on profile, clickable vote tally filters; bills browse topic quick-pick chips (`LANDING_TOPICS`); bill detail history expand/collapse (8 items), section header polish, Roll calls label, hearings empty copy; profile activity **Topics** filter (`?topic=` + hybrid LegiScan match); normalized Follow copy (`src/lib/follow-labels.ts`); nav tooltips toggle without secondary text; `diagnose:legislators` reports missing `legiscan_id`; `generate:district-thumbnails` (276 assets under `public/geo/district-thumbs/`); LegiScan reconcile seat-only fallback when Open States preferred names differ from LegiScan legal names (`ky-sync-pipeline.ts` — 0/138 missing after sync).
- **Doc / artifact hygiene (2026-05-22)** — Removed stale Framer handoff doc (`docs/framer-architecture.md`); stopped tracking LRC audit JSON under `reports/` (script output only; gitignored).
- **Verify-and-refine PR train (2026-05-21)** — All 13 recent-ship PRs PASS verification (Waves 1, 2a–d on the `verify-and-refine-app` branch); see `decisions.md` § 2026-05-21 for the linked GH-Actions vs Vercel-cron decision.
  - **#23** Committee calendar + meetings routes + civic UX — `/committees`, `/meetings`, bill-detail hearings block, `/legislature/resources` hub all render and link as specified.
  - **#24** De-duplicate bills sync + quiet hourly Slack — no duplicate `ky_bills` upserts in dry-run; Slack noise reduced to summary lines only.
  - **#25** Digest email refinement — `BillDigest` grouped by reason with raw actions shown and clarified labels (`preview:digest` render matches spec).
  - **#26** Uniform member cards + scalable district minimap — `LegislatorDistrictMinimap` on `MemberCard` + `/members/[slug]`; consistent card shell across surfaces.
  - **#27** Committee assignments + sponsored bills on profiles — `/members/[slug]` shows committees + sponsored bills (info alert removed when data present).
  - **#28** Wave 1 hero contrast + returning-user home — WCAG-AA primary CTAs; signed-in users see the Welcome-back hero on `/` (no auto-redirect).
  - **#29** Restore educational tooltips + nav toggle — `tooltipsEnabled` toggle visible in nav, persists, gates the custom `<Tooltip>` component.
  - **#30** Hover lift on bill cards — consistent in browse / search / feed / home (later refined by Wave 4b — preview wrapper removed).
  - **#31** Remove copy-link button from search — `BillsBrowse` filter bar no longer renders the button.
  - **#32** Lighter card hover shadow — `CivicCard` hover uses the new `CARD.hoverBoxShadow` token.
  - **#33** Normalize legislator card hierarchy — member and bill-sponsor cards share visual hierarchy / spacing.
  - **#34** Bill status chip tooltips — detail-page status-chip tooltip respects `tooltipsEnabled`; preview-style card tooltips removed in Wave 4b.
  - **#35** Schedule LRC committee calendar sync on GitHub Actions — `.github/workflows/sync-lrc-calendar.yml` runs at 12:00 and 18:00 UTC; secrets-free run path; Vercel `lrc-calendar` cron entry retired in Wave 4a (see `decisions.md` § 2026-05-21).
- **Verify-and-refine branch — Wave 4 sweep (2026-05-21)** — Follow-up fixes after PR-train verification on `verify-and-refine-app`:
  - **Wave 1.5** — Moved `pageItems` `useMemo` above the loadmore early return in `src/components/ui/PaginatedSection.tsx` (fixes `react-hooks/rules-of-hooks` lint error introduced 2026-05-17).
  - **Wave 4a — sweep fixes** — Removed `lrc-calendar` cron entry from `vercel.json` (GH Actions is sole scheduler); single retry-with-backoff on Wayback CDX list call in `scripts/backfill-lrc-calendar-wayback.ts`; deleted dead `src/components/home/HomeAuthGate.tsx`; appended `SLACK_WEBHOOK_SYNC` / `SLACK_WEBHOOK_URL` to the secrets doc-block in `.github/workflows/sync-lrc-calendar.yml`.
  - **Wave 4b — tooltip taxonomy enforcement** — Removed the whole-card preview `<Tooltip>` wrapper from `src/components/bills/KYBillCard.tsx`; deleted `KYBillCardTooltipTitle.tsx`, `BillTooltip.tsx`, `TooltipExamples.tsx`; audited `MemberCard` / `KYCommitteeCard` / `CommitteeMeetingCard` / `BillsListTable` (all remaining tooltips are educational); rewrote README `Tooltip system architecture` with the three-category taxonomy.
  - **Wave 4c — MemberCard → CivicCard** — Migrated `src/components/members/MemberCard.tsx` onto the `CivicCard` shell (header / body / footer slots); hover normalized via `CARD.hoverBoxShadow` token (absorbs F3); governor styling, stretch-link, and pointer-events preserved via `sx`.
  - **Wave 4d — Glossary page** — New `/glossary` page rendered from `governmentTooltips`, grouped + alphabetized with stable anchors; cross-references rendered as links; full plain-English editorial pass on all entries; 20 `KY_TOPICS` subjects added under a new `subject_topics` category; `TooltipContent` gains a `category` field; `Tooltip.tsx` gains an optional "Learn more" `InfoOutlined` affordance; footer link added; `billStatusExplanations` reconciled. See `decisions.md` § 2026-05-21.
- **Member profile data + Wave 1 home (2026-05-20)** — Committee assignments from LRC calendar on `/members/[slug]`; sponsored bills via JSON `cs` filter + `people_id` inference; fuzzy `legiscan_id` sync. Home: WCAG-friendly hero CTAs (solid white primary); signed-in **Welcome back** hero (feed/bills/map) without auto-redirect off `/`. PR #27 + wave-1 polish branch.
- **Browser review — remaining P1 (2026-05-19)** — Shared `BillNumber` (`detail` / `card` / `compact` / `inline`) on browse, detail, home lists, profile, committee agenda. `LegislatorDistrictMinimap` + lazy wrapper on `MemberCard` + profile. `LegislatorAvatar` party badge on bill sponsors, members, map tooltip, `SponsorAvatarChip`, design-system. Member profile: name-based sponsored-bill fallback in `member-profile-data.ts`; per-section empty states (votes still need `legiscan_id`). Committee browse: `fetchKyCommitteesBrowseEnriched` — leadership line + `KY_TOPICS` chips on `KYCommitteeCard`. Committee detail: bill-detail-style header card, breadcrumbs, at-a-glance, official-source buttons, compact legislative member list.
- **Browser review — low-lift UX (2026-05-19)** — Bill cards: removed `Tooltip` + hover lift on `KYBillCard` / `CivicCard` `variant="bill"`. Shared `BillStatusMetaChip` with Passed/Signed checkmark styling. Bills browse: dropped gavel on results row. Members: LegiScan footer removed from `MemberCard`; generic capitol phone hidden (`isGenericCapitolPhone`, `legislatorDisplayPhone`); executive sections merged as “Governor's office”; `Lt_governor` → Lieutenant Governor. Committee detail: no “Synced” caption; click meeting header to expand agenda. Frankfort resources: list layout, no intro paragraph, no per-card KYVKY links. Bill number typography: detail `h2`, grid `h4` (list/home/profile audit still open).
- **Roadmap Wave 1–2 (2026-05-19)** — TASKS reorg (Bill Watch / launch polish / committee Wave 3). Activity feed filters; notification groups + Bill Watch copy; saved searches (`025`); snooze, export, welcome resend, `/about`, map autocomplete, contact accordion; `/dashboard` → `/profile#activity`. See `docs/email-client-qa.md`, `docs/spikes/committee-materials-sync.md`.
- **Bill detail — server-rendered shell (2026-05-19)** — `/bills/[id]` loads bill + LegiScan enrichment on the server (`ky-bill-detail-server.ts`, `revalidate=300`, `generateMetadata`). Client `BillDetailView` handles follow, tooltips, roster match, deferred LRC link + hearings; no full-page spinner on first paint.
- **Performance — members roster + LegiScan cache (2026-05-19)** — `GET /api/roster/members` (cached, slim columns); members/map stop `select('*')` and chamber refetch. Bill detail LegiScan cached 5m; search uses slim bill columns; hearings lazy-loaded.
- **Bills browse sort UI (2026-05-19)** — `/bills` filter bar: Sort by dropdown + asc/desc toggle; `?sort=` / `?dir=` URL state (`ky-bills-browse-url.ts`); active chip + clear-all reset. Non-default sort uses the in-memory browse cap path (same as status/follows filters).
- **Committee calendar Phase 4 — hearing digest events (2026-05-19)** — LRC calendar sync writes `hearing_scheduled` to `ky_bill_status_history` when a followed bill is newly listed on a committee agenda (`ky-calendar-hearing-history.ts`, deduped by meeting + bill + agenda hash). Digest + profile activity use `formatDigestEventDetail` for calendar lines; `/legislature/resources` notes Bill Watch comparison. Opt in via **Hearing scheduled** on `/profile` (not in the default “Major milestones” preset).
- **Performance — browse server prefetch (2026-05-19)** — `/members`, `/bills` (+ house/senate), `/committees`, `/meetings` load list data on the server (`unstable_cache`, `revalidate` 60–300s). Bills browse skips client fetch when URL matches server prefetch; meetings omit `member_refs` on browse. `ky-ga-browse-server.ts`, `BillsBrowsePage.tsx`, `MembersBrowse.tsx`.
- **Performance — homepage + profile (2026-05-19)** — `/` is a Server Component shell (`LandingHero`, `LandingFeatures`, `LandingTopics`, `SessionBannerServer`); client islands: `HomeAuthGate`, `LandingMapSection` (dynamic Mapbox). Profile `ky_user_profiles` uses explicit column select (`KY_USER_PROFILE_SELECT`).
- **Performance — feed + search + committee detail (2026-05-19)** — `/feed` server-prefetches recent House/Senate bills (`KY_BILL_BROWSE_SELECT`, 120 rows cached) + slim roster; followed bills use slim select client-side. `/search` passes cached roster from server (no `/api/roster/active` on load). Committee detail: slim meeting/agenda selects, `unstable_cache` on slug/meetings/agenda batch (`ky-committee-data.ts`, `ky-ga-browse-select.ts`).
- **Performance — browse, search, middleware, homepage (2026-05-19)** — `GET /api/bills/browse` with SQL pagination + slim columns (default browse); in-memory cap 2,000 when status/follows/sort filters apply. Bill search: FTS-first via `ky_bills_plain_search`, supplemental legs only when FTS is thin or unavailable (replaces always-on 9-query fan-out). Middleware skips Supabase `getUser()` on anonymous public routes (`session-middleware.ts`). Homepage Mapbox moved to `dynamic()` (`LandingDistrictMapPreview`). See `decisions.md` § 2026-05-19 performance.
- **Performance — roster cache & committee batching (2026-05-19)** — `ky-legislator-roster-server.ts` (`unstable_cache` 1h): active slim roster for browse/search, active committee columns for `/committees/[slug]`, full roster for member profiles (historical rows preserved). `GET /api/roster/active` + `useKyActiveLegislatorRoster` (bills browse, search, feed, bill detail). Committee detail: batched agenda `IN` query (no N+1), `getKyCommitteeBySlug` + `revalidate=300`. Member profile: `React.cache` dedupes metadata + page roster fetch; `revalidate=300`. Bill detail API: roll-call enrichment cap 12; `Cache-Control` 5m.
- **Committee detail — members section (2026-05-19)** — `/committees/[slug]` lists members from LRC calendar `member_refs` + Open States `committee_memberships` fallback (`CommitteeMembersSection`, `ky-committee-members.ts`).
- **Committee calendar Phase 1 (2026-05-18)** — `024_ky_committee_calendar.sql`; `syncKyLrcCalendar` + `?source=lrc-calendar`; Vercel cron 12:00/18:00 UTC; CLI `npm run sync:ky:lrc-calendar`.
- **Committee calendar Phase 0 (2026-05-18)** — LRC calendar fixtures + HTML parser + bill-reference extractor; `spike:lrc:calendar` / `audit:lrc:bill-refs`; [phase0 report](./docs/specs/committee-calendar-phase0-report.md); Bill Watch reference at `docs/reference/bill-watch/`.
- **GA-only product scope + paused local crons (2026-05-18)** — Removed ordinances / school-boards / county-actions from `vercel.json`; `SYNC_SOURCES_DEFAULT` limits autopilot sync; [docs/specs/committee-calendar.md](./docs/specs/committee-calendar.md) + `decisions.md` § 2026-05-18.
- **Digest history on `/profile` (2026-05-13)** — New `ProfileDigestHistorySection` shows the user's last 10 sent digests with the bills + event labels included, so anyone who lost the email can still see what was sent. Backed by `GET /api/me/digest-history`: queries `ky_notifications_log` under the user's JWT (RLS-scoped), then uses `supabaseAdmin` to expand `event_ids` against the service-role-only `ky_bill_status_history` and join to `ky_bills` for labels. Dedupes bills per digest and caps to 10 with "…and N more" overflow.
- **Launch blockers — Sentry alerts + privacy + terms + email polish (2026-05-13)** — `/api/cron/notify` and `/api/webhooks/resend` now `Sentry.captureException` on thrown errors and `Sentry.captureMessage` on silent partial-failure 200s, tagged `route:`* for alert-rule scoping. New `/privacy` and `/terms` pages cover what we collect, who we share with, retention, user controls, and acceptable use; both linked from `SiteFooter`, the register page (acknowledgment line), and email footers. Outbound mail policy: `From: alerts@kyvky.com` (transactional only); `Reply-To: katie@kyvky.com` (real inbox, also used in privacy/terms contact). Plain-text fallback (`render(el, { plainText: true })`) included on every transactional send for deliverability + a11y. Per-user rate limits on `/api/bills/[id]/follow` (60/min) and `/api/me/preferences` (30/min) keyed by user id with 429 + Retry-After. `/profile` followed-bills now shows a CTA empty-state card (browse bills / find your legislators) when no follows.
- **Legislator stale row cleanup + Senate district format (2026-05-13)** — Members page was showing 381 active legislators when the General Assembly has 138 seats. Two fixes: `syncKyLegislators` now runs a second deactivation pass for active LegiScan-only rows at seats covered by a current Open States row (predecessors and alias dupes); `scripts/normalize-legislator-districts.ts` rewrites legacy Senate `SD-0XX` to canonical `SD-XX` so the seat-key compare actually matches. New `npm run cleanup:stale-legislators`, `npm run normalize:legislator-districts`, `npm run diagnose:legislators`, and `npm run debug:openstates-roster`. After running cleanup + normalize the live count drops to ~141 active (100 House + 38 Senate + 3 statewide).
- **Email copy / structure pass (2026-05-13)** — `BillDigest` subject line is count-led ("Your KY bill digest — N updates"); preview text names bills + updates; new footer adds "Change frequency or topics" alongside unsubscribe and a one-line "why am I getting this" attribution. `WelcomeEmail` lead clarifies cadence ("we'll only email when followed bills change — no marketing"); marked one-time so users don't expect repeats; topic-follow card mentions automatic matching.
- **Topic taxonomy — hybrid digest matching (2026-05-13)** — Added `src/lib/ky-topic-legiscan-mapping.ts` translating LegiScan subject patterns to KY_TOPIC tags so the digest cron matches a user's `topic_filters` against EITHER `ky_bills.topics` (heuristic) OR `legiscan_subjects` (official) — closes the silent-miss path where a bill's correct LegiScan subject didn't earn a heuristic topic tag. `BillTopicMatchHint` reflects the same union so UI agrees with the digest. New `npm run audit:legiscan-subjects` and a workflow step on the weekly job report unmapped subjects sorted by frequency so the mapping stays current.
- **Legislator links — full fidelity + weekly verifier (2026-05-13)** — Migration **023** adds `external_links` JSONB on `ky_legislators`. Sync persists the full Open States `links[]` (URL + note + category + host); `MemberProfileView` renders a "Connect & follow" section grouped Social / Other (hidden when empty). Verifier now probes `external_links` (skips social by default, `--probe-social` to opt in) and emits JSON with `--output`. New `.github/workflows/legislator-links-weekly.yml` runs Mondays 12:00 UTC: `sync:ky:legislators` → `verify:legislator-links` → `audit:legiscan-subjects`, uploading reports as artifacts.
- **Follow Bills — M3 polish (2026-05-13)** — `BillTopicMatchHint` on bill detail: when a signed-in user has digest enabled and the bill's topics intersect their `topic_filters` but they aren't individually following, an inline alert names the matching topic so users understand updates are already in their digest.
- **Follow Bills — M8 bounce + welcome (2026-05-13)** — Resend webhook (`/api/webhooks/resend`) verifies Svix signatures and updates `ky_notifications_log.delivery_status` + `ky_notification_preferences.{bounce_state,bounce_count,suppressed_at}` (migration **021**). Digest cron filters `suppressed_at` users; emails set `List-Unsubscribe` + `List-Unsubscribe-Post: List-Unsubscribe=One-Click`; `/api/unsubscribe/[token]` accepts both GET (HTML) and POST (RFC 8058). One-time welcome email after first verification: `WelcomeEmail` template + `/api/me/welcome-email` (idempotent stamp-then-send) fired from `/auth/verify` (migration **022**). New harness scripts: `npm run preview:digest`, `npm run verify:digest-state`, `npm run preview:welcome`. `scripts/load-env.ts` now falls back to the main repo when running inside a `.claude/worktrees/`* worktree and stubs `WebSocket` so supabase-js works on Node < 22.
- **Follow Bills — M5–M7 (2026-05-12)** — `ky_bill_status_history` with **pre-upsert** snapshots + `recordBillStatusHistoryForBuiltBatch` on all `syncKyBills` code paths; digest cron + Resend + unsubscribe route (`decisions.md` § 2026-05-12).
- **Follow Bills — M1 auth & profile foundation** — Migration `016_ky_user_profiles` + Auth sync triggers; MUI auth pages (`login`, `register`, `forgot`, `reset`, `verify`, `logout`); `/profile` **Account** + **Security** (password, email change, resend verification, typed-email delete); `DELETE /api/me/account`; post-login `/profile`; nav user menu Profile-first. Apply migration (`npm run db:apply-sql` or Supabase SQL) and configure redirect URLs for `/auth/verify` and `/auth/reset`. See `decisions.md` § 2026-05-11.
- **Sentry** — Example page and `/api/sentry-example-api` removed; production monitoring remains via `@sentry/nextjs` configs and `/monitoring`.
- **Tooltips** — `MemberCard` Ballotpedia / bill history segments respect global `tooltipsEnabled`; clerical-stage hint on timelines.
- **District map** — Kentucky state-shape nav icon (`KentuckyStateIcon`); exclusive House/Senate layer toggle; right-column **two legislators** explainer (**100 House / 38 Senate**); “How to contact your legislators” accordion; Mapbox-backed address lookup (biased to KY).
- **Votes & external links** — LegiScan roll call links where `roll_call_id` exists; Ballotpedia via `normalizeBallotpediaHref` / search helpers (`external-legislative-links.ts`).
- **Member profiles** — Sponsored bills and vote summaries (`fetchSponsoredBillsForLegislator`, `fetchMemberVoteRecord`).
- **UI polish** — Mobile nav Paper styling; hero CTAs; LRC committee schedule banner link; session status banner semantics; footer © / `APP_VERSION` / `/licenses`; bill detail sponsor avatars + sidebar spacing; curated bill list hides view count when appropriate.
- **Browse & search** — Bills browse `BROWSE_QUERY_ROW_LIMIT = 1000`; search merge cap aligned; **25 / 50 / 100** page sizes (`usePersistedPageSize`).
- **LegiScan subjects** — `legiscan_subjects` + `legiscan_subjects_search` (migration `015`), sync + search hooks.
- **Discovery & bill search (migration 017)** — FTS RPC `ky_bills_plain_search`; parallel search legs (`last_action`, `session`); multi-token relevance ranking (`ky-search-bills.ts`). LegiScan-backed search suggestion chips (`useKySearchSuggestionSubjects`, `ky_top_legiscan_subject_names`). Members roster committee filter (`?committee=`, `committee_memberships` from Open States roles + sync). Clearer non–bill-type search alert.
- **Verification snapshot (2026-05-11)** — Confirmed in repo: `supabase/migrations/017_search_members_discovery.sql`; app wiring in `src/lib/ky-search-bills.ts`, `src/lib/use-ky-search-suggestion-subjects.ts`, `src/lib/ky-committee-utils.ts`, `src/app/search/page.tsx`, `src/app/members/page.tsx`, `src/lib/ky-sync-pipeline.ts`. Auth recovery typing fix in `src/app/auth/reset/page.tsx` (`establishRecoverySession`). Latest `npm run build` succeeds.
- **Supabase apply** — Migrations **016–017** deployed on the primary project (2026-05-11): auth/profile (`016`); FTS bill search RPC, LegiScan subject RPC, and `committee_memberships` column (`017`). `sync:ky:legislators` run: URLs and committee arrays updated from Open States when `roles` / `links` are returned.
- **Sync CLI env loading** — `scripts/load-env.ts` resolves `.env` / `.env.local` from the repo root (not `cwd`) and applies `.env.local`** with **`override: true` so `OPENSTATES_API_KEY` and friends load reliably for `npm run sync:ky:`*.
- **Spot-check — links & discovery (2026-05-11)** — `npm run verify:legislator-links -- --limit 48`: 0 failures (LRC **200**, Ballotpedia **202**, LegiScan `getPerson`). `npm run spot-check:bill-links`: 12 recent `ky_votes` roll calls OK via LegiScan `getRollCall` (public legiscan.com/HTML returns **403** to automation—normal); 10 sponsor Ballotpedia URLs **202**. Production smoke (**www**): `/search` loads with suggestion chips (**education**, **budget**, **23**, **HB 1**); `/members` roster **381** after client fetch (member cards show KY Legislature / Ballotpedia / LegiScan links). Assistive snapshot before hydration can briefly show **0 people**—confirm count after load.

## Up Next

Roadmap priority (2026-06-02): **launch operator checklist** → **Wave 3 committee/data**. Waves 1–2, committee calendar Phases 0–4, and **Follow committees v1.5** are all shipped.

### Members roster views — fix/optimize → search, filtered, historical views (planned 2026-07-18)

Spec: **[docs/specs/members-roster-views.md](./docs/specs/members-roster-views.md)**. Phases 0 + 1 shipped on `claude/members-page-lag-pv5qef` — see Recently completed. Next: Phase 2 (URL-driven filters), then richer search, then historical/former-member views (**interpretation confirmed 2026-07-18: end-user views**; needs the `ky_legislator_terms` model — the roster table has no term dates today, only `active`). County filtering: **out of scope** (2026-07-18). Remaining open questions in the spec: committee-filter data path (gates 2b), former-member SEO (gates 4b). **⚠️ Operator:** apply migration **042** then run `npm run sync:ky:legislators` to activate profile slugs (deploy is safe before or after — code falls back cleanly pre-migration).

### Launch operator checklist (manual, not blocking code)

See **[docs/launch-checklist.md](./docs/launch-checklist.md)**.

### Wave 1 — Bill Watch parity (shipped 2026-05-19 — verified PASS 2026-05-21)

Reference: [docs/reference/bill-watch/](./docs/reference/bill-watch/README.md). Mapping: [bill-tracking.md](./docs/reference/bill-watch/bill-tracking.md).

**Blocked (product):** Add PNG captures to `docs/reference/bill-watch/screenshots/` per INDEX before pixel-polish passes.

- **Activity feed filters** — Shipped + verified 2026-05-21 (chips, `?kind=`, empty states).
- **Alert settings UX** — Shipped + verified 2026-05-21 (groups + Bill Watch link on `/legislature/resources`).
- **Saved searches MVP** — Shipped + verified 2026-05-21 (copy/save on `/bills` + `/profile#saved-searches`; migration **025** applied).
- **Bill tracking polish** — Shipped + verified 2026-05-21 (“Follow another bill”, `/dashboard` redirect); follow copy normalized 2026-05-22 (`follow-labels.ts`).
- **Design backlog (open)** — Home hero CTA contrast (Wave 4 ✅); member profile section order (2026-05-22 ✅); returning-user hero (2026-05-20 ✅); **Topics** activity filter (2026-05-22 ✅).
- **Bill card hover UX (browser review)** — **[P1] ✅ Wave 4b (2026-05-21)** — whole-card `<Tooltip>` wrapper removed from `KYBillCard`; preview-style tooltip components deleted; status-chip tooltip on bill detail remains as educational.

**Wave 1 non-goals:** Kentucky.gov auth, rules wizard, mobile quiet hours, per-bill alert overrides, premium “new bill match” email blast.

### Wave 2 — Launch polish (shipped 2026-05-19 — verified PASS 2026-05-21)

- **GDPR-style data export** — `GET /api/me/export` on `/profile` Security.
- **Resend "Send welcome again"** — `POST /api/me/welcome-email?force=1`.
- **Email rendering QA** (~2 hr, manual) — [docs/email-client-qa.md](./docs/email-client-qa.md); run after copy tweaks from review.
- **Snooze on follows** — Profile toggle; digest skips snoozed (needs **025**).
- **Map address autocomplete** — Shipped on `/members/map` (`DistrictMapExplorer` MUI `Autocomplete` + Mapbox suggest).
- **About page** — `/about` + footer link.
- **"How to contact your rep"** — Map accordion + resources links.
- **In-app notification badge** (~4–8 hr, lower priority) — not shipped.
- **Per-user digest timezone** (~4–6 hr) — deferred (Nov 2026 / open-rate data).
- **Regression cadence** — After large syncs or schema changes, re-run `npm run verify:legislator-links` and `npm run spot-check:bill-links`.

### Wave 3 — Committee / data (deferred)

From [docs/specs/committee-calendar.md](./docs/specs/committee-calendar.md) § Phase 5+:

- `ky_committee_materials`** + **`sync:lrc:committee-materials` — **Shipped 2026-06-02** (see Recently completed). Cron wired same day at **13:30 UTC daily** via Vercel (`vercel.json` `crons`). Historical prior-year backfill applied 2026-06-11 (`npm run backfill:lrc:committee-materials`). Slack notify follows the existing `ky_sources` pipeline.
- **Session record — enrollment actions (Phase 5b)** — **Shipped 2026-06-11** (see Recently completed). Parser + sync + cron; roll-call PDFs and per-status bill-list pages remain deferred per [session-record-spike-report.md](./docs/specs/session-record-spike-report.md).
- **Interim period + session milestones** — **Shipped 2026-06-01** (see Recently completed).
- **LRC bulk API** — revisit if state publishes machine-readable roster (see Backlog below).

**Wave 3 note:** Follow committees **v1 shipped PR #38**; remaining gaps are **v1.5** (see Backlog), not Wave 3.

## Backlog

### `/members` page redesign — replace governor's-office photos with search/filter tiles

Note-to-self, 2026-07-18: the three photos of the governor's office on `/members` should be replaced with tiles that let a visitor search the House and Senate sides. Those tiles become anchor links down to the senators/representatives listed below on the same page. Later pass: fill in real search filters (chamber, party, district, name), and backfill as many members as possible so search/filter coverage is complete.

Update 2026-07-19: step one shipped — the Governor's office section is removed and `/members` lists House and Senate only (executive officials keep their profile pages and stay in the shared roster for other surfaces). The tiles/search build below is still open.

Update 2026-07-20 (branch `claude/tasks-md-cleanup-y64yk7`): tiles + party filter shipped. Two **chamber jump tiles** (House / Senate) now sit where the governor's-office section was — each shows the current (filtered) member count and is a native `#members-house` / `#members-senate` anchor link down to that chamber's list (`scrollMarginTop: 96` clears the sticky header; keyboard-focusable, works without JS). Tiles show only in the default two-section view; a chamber left with zero matches renders a dimmed "No matching members" card instead of a dead link. Added a **party filter** (`Republican` / `Democrat`, data-driven — only appears when >1 party is present) beside the existing chamber toggle + name/district search, with a matching active-filter chip and "Clear all". Verified end-to-end against the `scripts/perf` PostgREST stub (140-member roster) with Playwright: tile counts (100/38), anchor scroll, party filter (Democrat → House 25 / Senate 9), dimmed-tile branch, and tile-hide-on-chamber-toggle all confirmed; tsc + eslint + `next build` clean. `src/components/members/MembersBrowse.tsx`. Filter state is still React-only — the spec's Phase 2a (URL-driven, shareable/back-safe filter state) and Phase 3a (richer search: role/committee/district-alias matching + highlighting) remain open follow-ups.

Card polish 2026-07-20 (same branch): each browse card's district thumbnail now **mirrors the profile page's statewide KY locator** (`KentuckyDistrictLocatorMap`, gained a non-interactive `interactive={false}` + caption-less mode so it can embed inside the card's stretch-link without a nested anchor) instead of the old zoomed Mapbox crop, and sits **side-by-side** with the portrait/name/title rather than stacked below (scaled to ~100–124 px). Tightened the contact area: the default per-card link row (KY Legislature / Ballotpedia / …) moved out of the bottom-aligned footer to sit directly under the email, so a card now has **one** divider (header → contact) instead of two. Profile page (`showDistrictMinimap={false}` + `footerContent`) and the district-map explorer card (`showDistrictInSubtitle={false}`) are unaffected. `src/components/members/{MemberCard,KentuckyDistrictLocatorMap}.tsx`.

Icon + section polish 2026-07-20 (same branch): dropped the decorative leading icons from the browse chamber jump tiles + the "House of Representatives"/"Senate" section headings, and from the profile page's "Voting record" and "Committee assignments" headings. Sponsored-bills section (`MemberSponsoredBills`) gained a text **search** box (by bill number/title, mirroring the Voting-record search) and **load-more pagination** (`PaginatedSection`, 12/page) so a prolific sponsor's list no longer runs the page long; the existing result-count line stays. `src/components/members/{MembersBrowse,MemberProfileView,MemberSponsoredBills}.tsx`.

- [x] Design + build House/Senate search tiles for the space where the governor's-office section sat.
- [x] Wire tiles as anchor links to the corresponding member list sections below.
- [x] Add search filters (chamber, party, district/name) once the tiles are in place. — chamber toggle + name/district search already existed; **party** filter added 2026-07-20 to complete the set. (URL-driven filter state is spec Phase 2a, still open.)
- [ ] Expand member search to match cities/towns/counties (owner request 2026-07-19): map a place name to the district(s) covering it so "Bardstown" finds its representative and senator, not just name/district text matches. Needs a place→district dataset (county subdivisions vs. district boundaries — districts split counties, so a place can map to several districts).
- [ ] **⚠️ Operator — social links backfill:** run `npm run sync:ky:legislators` after the 2026-07-19 members branch deploys. The sync now harvests social handles (X/Twitter, Facebook, …) from Open States `ids`/`extras`/`other_identifiers` in addition to `links[]` (KY's `links[]` has zero social entries — verified against production data 2026-07-19). If X/Twitter coverage is still 0 after the resync, Open States simply has no KY social data and we need an alternative source (Ballotpedia scrape or manual curation) — owner decision.
- [ ] Backfill any missing members so filtering has full coverage.

### `ky_votes` duplicate rows — sync-side dedupe + one-time cleanup

Found 2026-07-17 while merging roll calls into the bill timeline: **152 duplicate groups** in `ky_votes` (same bill + date + yea/nay/absent), two flavors — (a) 51 rows with NULL `roll_call_id` that a later sync re-inserted with the id populated (two sync paths, no shared upsert key), and (b) LegiScan shipping one physical roll call twice under the same RCS#/RSN# with variant descriptions ("Third Reading" vs "Third Reading W/SCS 1"; a mislabeled "Veto Override" copy with identical tally/date). The bill page now dedupes at read time (`fetchDbVotes`), but the member-profile RPC (`get_votes_for_legislator`) and any other consumer still see raw rows.

- [x] **One-time cleanup — ✅ ran against primary 2026-07-17.** 135 rows deleted (51 NULL-`roll_call_id` twins + 84 same-RCS# copies), all preserved in `ky_votes_dupe_backup_20260717`. 6,944 votes remain; the 27 groups sharing a date+tally under **different** RCS numbers were confirmed as genuinely distinct roll calls and kept. Drop the backup table once a few weeks pass without complaints.
- [x] **Sync-side guard — ✅ done same day.** New `src/lib/ky-vote-dedupe.ts` (`dropDuplicateRollCallRows`): a roll call's physical identity is (bill, date, yea, nay, absent, RCS/RSN number parsed from desc); incoming rows duplicating that identity under a different `roll_call_id` are skipped, in-batch and against existing DB rows. Wired into both write paths — `syncKyVotes` and the dataset importer's `upsertVoteRows`. Rows without a parseable number are never cross-collapsed.
- [x] Read-time dedupe in `fetchDbVotes` kept as a guard, and made RCS#-aware so it can't collapse the 27 legitimate same-tally pairs.

### Backfill pre-2018 roll-call votes (`ky_votes`) — enables member Voting-record history

The member profile now has a **shared legislative-session selector** (2026-07-17) covering both Sponsored bills and the Voting record. Sponsor data exists for every session (2010–2026), but roll-call `ky_votes` only exist from **2018 Regular Session onward** — for any earlier session the member's Voting record renders empty ("No recorded votes found for this session yet.") even though sponsored bills show. Coverage as of 2026-07-17 (votes per session): 2018 RS 799, 2019 RS 675 / Special 11, 2020 RS 605, 2021 RS 816 / Special 33, 2022 RS 946 / Special 4, 2023 RS 678, 2024 RS 844, 2025 RS 701, 2026 RS 967; **2010–2017 RS + specials = 0 votes**.

- [ ] Backfill `ky_votes` for **2010–2017** regular + special sessions (and the empty 2018 Special) so the pre-2018 Voting record isn't blank. **⚠️ The original plan here — "prefer the LegiScan Dataset Pull API path" — is RETRACTED as of 2026-08-01: it cannot work, and the dataset importer is not at fault.** The forced full re-import ([run 30676976863](https://github.com/ktoepp/know-your-vote-kentucky/actions/runs/30676976863), `sync:ky:dataset --force`, all 25 sessions) logged a clean upstream break — 2018 RS decoded **799 roll-call files**, 2018 Special and *every* earlier session decoded **0**, while their bills/history/texts all imported at 100%. LegiScan ships roll-call JSON in the dataset ZIP only from the 2018 Regular Session onward, so re-running the reconcile will never fill these sessions no matter how often it runs. Replacement path (PR #224, `npm run backfill:session-votes`): the pre-2018 *bill* payloads still carry a `votes[]` summary holding the `roll_call_id`s, so recover the ids from there and buy each with `getRollCall` — the only call that returns the per-member breakdown `ky_votes.roll_call` needs for member Voting-record pages (tallies alone would light up the bill page and leave `/members/[slug]` exactly as empty as today).
  - [ ] **Unverified premise, and the first thing to settle:** whether `votes[]` is actually populated in pre-2018 bill payloads. Nothing in the DB records it, and legiscan.com blocks automated fetches. `npm run backfill:session-votes -- --since-year=2015` (discovery only, ~5 quota points, no writes) answers it. If it returns zero ids, LegiScan has no pre-2018 KY roll-call record at all and this needs a different source (LRC record votes) or an accepted-limitation note on the member profile.
  - [ ] The workflow (`backfill-session-votes.yml`) is dispatch-only and **`workflow_dispatch` doesn't register until the file is on `main`** — dispatch 404s until PR #224 merges.
- [ ] After backfill, the session selector needs no code change — it already lists any session the member sponsored in; confirm the Voting record populates for a long-serving member across the newly-seeded sessions.
- [ ] Consider whether the selector should also surface sessions where a member has **votes but no sponsorships** (currently sponsor-based via `fetchMemberSessionsForLegislator`); revisit only if backfill surfaces such gaps.

### Accuracy-audit follow-ups (delegated — LLM / heavy verification out of scope for the deterministic agent)

The weekly `audit:accuracy` agent intentionally stays deterministic and cheap (see decisions.md § 2026-06-03). The following need semantic judgment or heavy fetches and should be handled by a human or a dedicated LLM/data agent, then cross-checked against primary sources before any content edit:

- **DONE (2026-07-05) — weekly-run triage: all 7 LLM warnings resolved (run seed=1288069341).** HB257 ai_summary flag was a **checker false positive** — the audit clipped descriptions to 800 chars, hiding the exact clause that grounds the summary; `llm-review.ts` description clips raised to 2,000 for both legs. Classifier gaps confirmed + fixed: `forcible entry and detainer`/`forcible detainer` → Housing (HB534), `physician`/`physicians` → Healthcare (SB98); backfill applied to primary (22,547 scanned, **153 changed**). Glossary: `hb`/`sb` now name the § 88 sign/veto/become-law-unsigned outcomes + override; `timeline_markup` reworded to KY committee-amendment/substitute terms; `senate_district` gains one-Senator-per-district (the model's "incorporated areas" objection was itself wrong — sentence kept); `timeline_vote` aligned to the 2026-06-08 `floorVote` "members present" fix it had missed. HB44 "Judiciary topic missing" **declined** as a taxonomy product decision (see next item). Round 2 (pinned-seed re-runs): `agricultural`/`cattle`/`cattlemen` → Agriculture (HB294; second backfill **138 changed**), HB877 summary regenerated (VA legal-term precision; new `--bill=` flag on `backfill:bill-summaries`), `timeline_markup` key renamed `timeline_committee_amendments` (internal key leaked into the audit prompt); SB168/Tabled/HB860 warns dismissed per the advisory cap. See decisions.md § 2026-07-05.
- [x] **Taxonomy call: add a `Judiciary` topic? — ✅ ADDED (2026-07-05, same day).** Corpus count answered the HCR84 test: **370 bills** form a coherent unmet cluster. Narrow keyword set (bare `judge`/`court` excluded — judge-executive/fiscal-court traps), anchored LegiScan subject patterns, `topic_judiciary` tooltip. Same PR added a **boilerplate pre-strip** to `classifyTopics` (committee-referral clauses, compact separation-of-powers language, "United States Supreme Court") after spot-checks caught 26 would-be false positives. Backfill applied to primary (370 changed); `/bills?topic=Judiciary` verified in-browser. HB44 now `["Judiciary","Voting Rights"]`. See decisions.md § 2026-07-05 — Judiciary topic.

- **DONE (2026-06-11) — topic classifier pass #3 + NV/absent glossary follow-up.** Removed bare `drug` from Healthcare (matched controlled-substance language in criminal/search-warrant bills); added `financial literacy` → Education; added body-worn camera / search warrant / electrical shock / swimming pool → Public Safety; `warrantless` / `no-knock` → Criminal Justice. Backfill: **22,547 scanned, 2,392 changed**. NV/absent tooltips now scope the 51/20 threshold to final passage (third reading) with procedural-vote caveat (follow-up to PR #74 / § 2026-06-08 pass). Manual drift tool: `npm run refresh:bill-status` (re-fetch LegiScan detail + mapper when hash-gated sync skips unchanged bills).
- **DONE (2026-06-03) — classifier keyword precision.** Removed the three dominant false-positive keywords in `src/lib/ky-topic-classifier.ts`: bare `emergency`/`safety` → Public Safety (matched "emergency clause" boilerplate; replaced with specific phrasing like `emergency management`/`first responder`/`public safety`), `commissioner` → Local Government (matched state offices like Commissioner of Agriculture / Insurance Commissioner; replaced with `magistrate`/`county judge-executive`), and `fiscal` → Taxation ("fiscal court/note/year" noise). Backfilled existing rows via `scripts/reclassify-bill-topics.ts` (22,547 scanned, 2,536 changed, ~2,548 bogus tags removed). Validated before/after on the LLM-flagged bills (SCR192/SB239/SB3/HB12).
- **DONE (2026-06-03) — triage pass #2 + Voting Rights precision + HCR84 call.** LLM dry-run across seeds 1/2/3/4/7/11/99 at sample 40–80 returned **0 warnings** (content already clean). Direct corpus triage then found two more bare false-positive keywords: `registration` (radon/metal-detector/vehicle/firearm/motorboat registration) and `primary` (primary care/school/residence) → Voting Rights. Replaced with `voter registration`/`primary election` and added plural-aware `elections`/`voters` (word-boundary match missed plurals that genuine "relating to elections" bills use). Backfill: **22,547 scanned, 648 changed (343 removed, 227 added)**, 0 new false positives. `HCR84` taxonomy call **resolved: declined a "Natural Resources" topic** — the 2010 coal/oil/gas-industries caucus is defensibly `Energy` (matches the site's own Energy definition), and the ~620-bill extractive cluster is already served by Environment/Energy/Taxation with no coherent unmet group; a 21st overlapping user-facing topic would be a sweeping re-tag. Also confirmed `ai_summary` is null for all 22,547 bills (faithfulness leg is dormant) and the glossary verified clean against Ky. Const. §§ 55/88. See decisions.md § 2026-06-03 (triage pass #2). Remaining topic nuance is advisory (LLM).
- **LLM review pass (`llm-review.ts`) findings are advisory.** `ai_summary` faithfulness, `topics[]` plausibility, and glossary correctness must be verified against primary sources before edits. The model's own suggestions can be wrong (2026-06-03: it proposed incorrect veto/emergency-clause thresholds). Triage cadence for these flags is TBD. The glossary pass now covers both `governmentTooltips` and `voteCountTooltips` (vote-count labels + `topic_*` subject descriptions); `reviewSummaries` is a no-op while `ai_summary` is unpopulated. See decisions.md § 2026-06-08.
- **DONE (2026-06-08) — tooltip accuracy pass: 23 entries corrected in `src/lib/tooltipContent.ts`.** Full LLM review pass across 8 seeds; all substantive glossary findings triaged and resolved. Entries fixed: `yea` (§ 46 majority-of-members-elected; procedural-vote caveat), `nv` (one-fewer-Yes framing, not counts-as-No), `absent` (same as nv), `concurrence` (conference follows refusal + recede option + bill may die), `floorVote` (members present, not all 100/38), `lrc` (KRS 7.090 statutory committee), `hjr`/`sjr` (Ky. Const. § 56: substantive JRs go to Governor; procedural ones don't), `first_reading` (§ 46 three-separate-days is a real constraint), `third_reading` (second-chamber passage goes to Governor, not "other chamber"), `sponsor` (introduced, not wrote; LRC does drafting), `cosponsor` (introduced, not wrote), `speaker`/`senate_president` (chosen by chamber members, not public election), `unanimous_consent` (removed US-Congress "extending debate time" example), `fiscal_note` (KRS 6.955–6.960 citation; removed disputed preparer attribution; "when prepared" not "typically"), `chaptered` (Acts of the GA, not KRS; LRC codifies separately), `signed_by_governor` (Acts of GA, not KRS), `enacted` (veto override as third enactment path), `floor_amendment` (members of that chamber), `motion_to_reconsider` (prevailing side), `timeline_introduced` (referred by Speaker/Senate President, not automatic), `engrossed` (clean copy framing), `ballotpedia` (removed "nonprofit" and "voting record" claims). See decisions.md § 2026-06-08. Residual warns after iteration are contradictory-across-seeds (chaptered 90-day, fiscal note preparer) or marginal omissions — dismissed per the advisory cap.
- **Bill-text / amendment body diff — not built.** Comparing stored vs. live statutory text requires `getBillText` (extra LegiScan call per bill, base64 docs) — too heavy/rate-limited for the always-on agent. Build as a separate, low-frequency pass if drift is suspected.
- **Committee-membership deep reconciliation — DONE (2026-06-09, PR #84).** See decisions.md § 2026-06-09 — Committee membership reconciliation. Canonical slug writing in sync, LRC-primary resolution on member profile, audit checker coverage added, `npm run reconcile:committee-memberships` diagnostic script. Recommittal mapper bug fixed in same PR.
- **Per-member roll-call identity.** Votes checker verifies tallies match `getRollCall`; per-member yea/nay *identity* drift (who voted which way) is not individually diffed beyond the summary tally.
- **DONE (2026-06-04) — Status mapper preserves furthest-progress milestone.** Added a `CHAMBER_PASSED_CODES = {2, 3, 4}` guard in `src/lib/map-legiscan-bill-status.ts`: when the LegiScan status code is Engrossed/Enrolled/Passed, an action text containing "committee" or "referred to" no longer regresses the stored status to `In Committee`. Eliminates the ~4% false-positive `fail` rate in the bills accuracy checker for post-session bills referred to interim committees; DB rows correct on next scheduled sync. See decisions.md § 2026-06-04.

### Follow committees v1 + v1.5 (archived reference — fully shipped)

**v1 (2026-05-22, PR #38):**

- Migration **026**: `ky_committee_follows` + `ky_committee_events` + RLS
- `GET/POST/DELETE /api/committees/[id]/follow`; committees on `GET /api/me/follows`
- `FollowCommitteeButton` on `/committees/[slug]`; bookmark toggle on browse cards
- `ProfileFollowedCommitteesSection` on `/profile`
- Digest: **Committee meeting scheduled** preference + committee block in `runBillDigestCron`
- LRC sync: `meeting_scheduled` events on new meetings
- Migration **027**: interim/statutory committee seed; `scripts/backfill-interim-calendar-2026.ts`

**v1.5 (2026-06-02):**

- Migration **028**: dedupe index relaxed to include `agenda_content_hash` so multiple `agenda_updated` rows per meeting persist
- LRC sync: `agenda_updated` (hash diff) + `meeting_cancelled` (post-loop diff with empty-fetch + Wayback guards) event types
- `/meetings?follows=me` filter (mirror of `/bills?follows=me`)
- Committee events in `GET /api/me/activity` as `committee_event` kind; `Committees` chip on `ProfileActivitySection`
- Follow toggle on `CommitteeMeetingCard` + agenda search rows (browse-level fetch, not per-card)
- Three event slugs (`committee_meeting_scheduled` / `committee_agenda_updated` / `committee_meeting_cancelled`) under **Committee & interim** in `/profile` notifications
- `EmptyState.message` widened to `ReactNode`
- Activity-row visual refinement: shared `ActivityStatusChip` with tone-mapped icon, `body1` titles, clickable committee names; strip CTA → `/profile#activity` ("View all activity")
- Spec: [docs/specs/follow-bills.md](./docs/specs/follow-bills.md) § "Follow committees (v1 + v1.5)"

### Follow committees (original roadmap — archived reference)

**Goal:** Follow committees and get notified on schedule/agenda changes — analogous to bill follows. **Fully shipped 2026-06-02** (v1 PR #38, v1.5 follow-on).

**Original technical sketch (fully implemented):**

- **Schema:** `ky_committee_follows` + `ky_committee_events` — **Done (026, 028).**
- **Diff capture:** `meeting_scheduled` + `agenda_updated` + `meeting_cancelled` — **Done.**
- **API:** `/api/committees/[id]/follow` + `/api/me/follows` + activity feed — **Done.**
- **Copy/spec:** Extend follow-bills spec — **Done (2026-06-02).**

**Prerequisites:** Committee kind tags + subcommittee parent (UI-8, UI-11) — **Done.**

### Committee calendar (GA) — from [docs/specs/committee-calendar.md](./docs/specs/committee-calendar.md)

- **Phase 0** — **Done 2026-05-18.** Fixtures, parsers, spike + audit scripts, [phase0 report](./docs/specs/committee-calendar-phase0-report.md).
- **Phase 1** — **Done 2026-05-18.** Migration `024`, `src/lib/ky-lrc-calendar-sync.ts`, `npm run sync:ky:lrc-calendar`, cron `lrc-calendar` 12:00/18:00 UTC.
- **Phase 2** — **Done 2026-05-18.** `/committees`, `/meetings`, bill detail “Hearings & agendas”, `/legislature/resources`, nav + footer links.
- **Phase 3** — **Done 2026-05-18.** Profile activity feed (`ProfileActivitySection`, `GET /api/me/activity`); agenda search (`/meetings?q=`); saved filters via URL (`?chamber=`, `?when=`).
- **Phase 4** — **Done 2026-05-19.** `hearing_scheduled` events from LRC calendar sync; resources page Bill Watch note.
- **Phase 5+** — See Wave 3 above (materials, session record, milestones).

### Other

- **Legislator links — full fidelity** — **Shipped 2026-05-13** (migration 023, sync, profile UI).
- **Legislator links — verifier in CI** — **Shipped 2026-05-13** (`.github/workflows/legislator-links-weekly.yml`).
- **LRC vs structured APIs (legislator data strategy)** — Kentucky LRC / `legislature.ky.gov` is the **canonical public-facing** source, but it does **not** provide a stable, documented **bulk API** for the full chamber roster the way Open States does. The app **syncs** from Open States into `ky_legislators` and **points users to the LRC** with stored profile URLs and official House/Senate directory fallbacks (`kyLegislaturePublicUrl` in `src/lib/ky-member-utils.ts`). **Revisit** if the state publishes **machine-readable bulk data** (CSV, API) with clear reuse terms.
- **"Follow this bill" — email alerts** — Full spec: [docs/specs/follow-bills.md](./docs/specs/follow-bills.md). Login-only follows, daily digest default, factual content (no AI summaries in v1), Resend + canonical site `kyvky.com`. Phased milestones:
  - **M1 — Auth polish & profile foundation:** **Complete.** `ky_user_profiles` migration `016`, Supabase sync triggers, auth/forgot/reset/verify flows, `/profile` Account + Security, account deletion API, login → `/profile`, Profile-first nav. Cookie-session middleware + auth layout/register polish. See Recently completed and `decisions.md` § 2026-05-11.
  - **M2 — Data model for follows:** **In repo** — migration `019_ky_follow_bills_schema.sql` (`ky_bill_follows`, `ky_notification_preferences`, `ky_bill_status_history`, `ky_notifications_log` + RLS). **Apply 019** on all deployed DBs.
  - **M3 — Follow UX (inline, no dashboard):** **Complete.** Follow button; `?follows=me` + Following filter on browse; `KYBillCard` bookmark + topic chips; profile lists. `BillTopicMatchHint` shown on bill detail when topics match user filters but bill isn't individually followed. KY_TOPICS ↔ LegiScan subject hybrid mapping (`src/lib/ky-topic-legiscan-mapping.ts`) used by both digest filter and the hint so they agree.
  - **M4 — Preferences UI:** **Complete.** Notification panel on `/profile` (frequency, event presets/checkboxes, topic grid, followed bills); `GET/PATCH /api/me/preferences` wired (2026-05-22).
  - **M5 — Diff capture in sync:** `recordBillStatusHistoryForBuiltBatch` after bill upserts with **pre-upsert** `fetchBillHistorySnapshots` (hash-gated + legacy `syncKyBills`). Dedupe via `legiscan_change_hash` + unique index.
  - **M6 — Email plumbing:** `RESEND_`* + `BillDigest` template + `/api/unsubscribe/[token]`. Domain verification / `WelcomeEmail` optional follow-ups.
  - **M7 — Digest cron:** `/api/cron/notify` (Bearer cron secret); Vercel `0 11 * * `*; `ky_notifications_log` idempotency.
  - **M8 — Launch hardening:** **Complete.** Bounce / complaint webhook + suppression (migration 021); digest cap (10 events + "and N more"); copy review (subject line, footer); `List-Unsubscribe` + `List-Unsubscribe-Post: List-Unsubscribe=One-Click` headers; one-time welcome email after verification (migration 022). End-to-end harness: `npm run preview:digest`, `npm run verify:digest-state`, `npm run preview:welcome`.
  - **Follow-up — verify digest send time:** After first DST transition (Nov 2026) or once open-rate data exists, evaluate whether `0 11 * * `* UTC is still the right hour. Consider open rates by hour, user feedback, and whether to add a per-user time-zone preference.
  - **Investigation — official vs. inferred topic taxonomy:** **Resolved 2026-05-13.** Outcome: Option A (hybrid digest match) shipped via `src/lib/ky-topic-legiscan-mapping.ts`. Preferences UI keeps the 20 KY_TOPICS (no LegiScan-subject picker — rejected as power-user feature). AI-fallback tagging deferred unless `npm run audit:legiscan-subjects` shows bills missing from BOTH taxonomies. Coverage maintained via the audit step on the weekly workflow.
- **Address search UX on map** — Shipped: Mapbox autocomplete on `/members/map` (2026-05-18 committee calendar ship); empty vs no-district states (2026-05-22).
- **"How to contact your rep"** — District map accordion covers basics; expand with capitol workflows, hearings, testimony links to LRC as product needs evolve.
- **PostHog — MCP follow-up (optional)** — SDK on main (2026-06-01): `instrumentation-client.ts` init, `PostHogPageviewTracker` for App Router pageviews, `src/lib/analytics.ts` (autocapture on, identified-only profiles). **Named user-action events shipped 2026-06-08 (PR #78):** `user_registered`, `bill_followed`/`bill_unfollowed`, `preferences_saved`, `account_deleted`. Set `NEXT_PUBLIC_POSTHOG_KEY` in Vercel to activate. **Optional later:** run `npx @posthog/wizard mcp add` to attach the PostHog MCP to Claude Code for in-terminal querying of insights, funnels, and feature flags. Worth doing once 1–2 weeks of event data have accumulated — the MCP is data-driven and adds nothing until then.

## UX design tracker (agent)

Active:

- (none)

Done:

- **UI-8 — Committee type tags (2026-05-22)** — `CommitteeKindChip` + `resolveKyCommitteeKind()` (LRC `committee_type` + name patterns: standing, subcommittee, interim joint, statutory, board, oversight). Wired on `CommitteeMeetingCard`, `KYCommitteeCard`, `CommitteeDetailView`, member profile committee tiles.
- **UI-9 — De-emphasize outbound LRC / external links (2026-05-22)** — `OfficialSourceLinks` text-link row replaces button stacks on committee detail, meeting card footer, member profile Connect & follow.
- **UI-10 — Member list: title + district on separate lines (2026-05-22)** — `LegislatorIdentityBlock` split `roleTitle` / `districtLine`; `MemberCard` roster listings use two-line subtitle.
- **UI-11 — Committee detail: merge At a glance + subcommittee context (2026-05-22)** — Stats folded into header overview card; `resolveKyCommitteeParent()` links subcommittees to parent standing committee when roster match exists.
- **Home hero Lottie + capitol background (2026-05-25)** — `HoverLottie` on landing feature icons; optimized capitol hero image; `LandingHeroCtas` extracted (`37c881a`).
- Home IA (2026-05-09): orientation-first hero primary CTA (district map), merged topic module, bill-area-only loading spinner — see `decisions.md`.
- Members (2026-05-09): filtered roster `profileHref` bugfix; member profile `h1`/`h2`/`h3` outline, `Link` back control, bill links identifiable by underline; roster card keyboard profile navigation + portrait alt + refresh `aria-label` — see `decisions.md`.
- Legislator outbound links (2026-05-09): link ranking + normalization in code (`legislator-link-normalize.ts`, sync + read paths); primary DB refreshed via `sync:ky:legislators` — spot-check with `npm run verify:legislator-links` — see `decisions.md`.
- **Link verifier:** `npm run verify:legislator-links` (`scripts/verify-legislator-external-links.ts`) — HEAD/GET checks for LRC, website, Ballotpedia, LegiScan URLs per active legislator.

Blocked:

- **Bill Watch screenshots** — Product to add PNGs under `docs/reference/bill-watch/screenshots/` (INDEX ready). Unblocks visual polish only; Wave 1 features can ship without them.

Notes:

- Designer-assisted UI/UX work follows user-provided Operating Principles (clarity before cleverness; explicit hierarchy; IA before layout; WCAG AA baseline; friction intentional; defaults and states explicit). Modes: Generative vs Critique per request. Conflict resolution: hierarchy accessibility > clarity > safety > consistency > efficiency > aesthetic refinement; state trade-offs and decision questions when ambiguous. Log substantive design decisions to `decisions.md` (append-only). See also `README.md` (Local maintenance scripts) for current `npm run` tooling.

Open design questions moved to **Wave 1 — Design backlog** (Up Next).

## Deferred / Decided Against

- **Local government sync (product surface)** — **Paused 2026-05-18.** Ordinances (Louisville/Lexington Legistar), school boards (JCPS/FCPS), and county/city council calendars (Jefferson/Fayette) are out of scope until GA committee work ships. Code remains; re-enable crons in `vercel.json` (schedules in committee-calendar spec). Manual: `GET /api/sync?source=ordinances` etc.
- `/events`** as mixed local meetings hub** — Hidden from nav; repurpose for GA committee meetings or replace with `/meetings` in Phase 2.
- **Executive orders sync** — Deferred. `governor.ky.gov` listings are unreliable for automated sync (404s, client-only rendering). Scraper exists in `src/lib/ky-executive-orders.ts` but is excluded from product surface and cron. Revisit when a stable feed/API exists.
- **Filibuster / cloture / budget reconciliation tooltips** — Removed. Federal Congress concepts only; do not re-add.

