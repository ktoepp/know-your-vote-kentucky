/** Row shape for `public.ky_user_profiles` (Supabase). */
export interface KyUserProfileRow {
  user_id: string;
  display_name: string | null;
  email: string;
  email_verified_at: string | null;
  created_at: string;
  updated_at: string;
}
